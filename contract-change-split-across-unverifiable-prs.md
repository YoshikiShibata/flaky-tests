# 単独で検証できない単位への PR 分割が、統合時にしか現れない不整合を「フレーク」として噴き出させる

## 概要

ある値の**意味**（契約）を変える変更を、

- PR-A: 契約の記述だけを変える（スキーマのコメント・仕様書・定数名・単位の定義など。**テストを 1 本も含まない**）
- PR-B: 実装をその意味に追随させる（自分の単体テストだけ更新）
- PR-C: その値を外部から検証する結合テスト／E2E を追加する（**PR-A 以前の意味で期待値を書いている**）

の 3 本に分割し、それぞれが**互いの変更を base に含まないまま**順に統合ブランチへマージすると、
各 PR の CI はすべて緑なのに、統合後の組み合わせだけが 100% 失敗する状態が生まれる。

この失敗は、統合ブランチを取り込んだ人のところで「**長時間ループの N 回目で突然落ちた**」という
フレークそっくりの顔をして現れる。実体はフレークではなく、**どの CI も一度も検証していない組み合わせ**が
初めて実行されただけである。

核心は「PR を細かく分割したこと」ではなく、**分割した各 PR が単独では検証不能だったこと**にある。
テストを含まない契約変更 PR は、その正しさを CI が確認する手段を持たない。実装だけの PR は、
準拠すべき契約の根拠を自分の中に持たない。検証は「3 本が揃った状態」でしか成立せず、
その状態は誰の CI にも存在しない。

## 症状

- 同一のテストスイートを連続実行するループで、**特定の 1 周目から急に**同じテストが落ち始める。
- 落ちるのは 1〜数個のテストで、差分は「ある数値フィールドの期待値だけ」。
  他のフィールド（レベル、合計量、時刻など）は完全に一致している。
- 失敗メッセージが「値が真逆・補数関係」になっていることが多い（例: 期待 250000 / 実際 750000 で、
  和が満点の 1000000）。時刻依存でも並行実行依存でもない。
- 落ち始めた周から**毎回**落ちる（再実行で直らない）。にもかかわらず、
  しばらくして誰かが統合ブランチに 1 行修正を入れると、何もしていないのに直る。
- 失敗ログには競合・タイムアウト・順序依存の痕跡が一切ない。

```
    xxx_test.go:72: Foo mismatch (-want +got):
          "plant": {
        - 		"current_level_progress_x1000": uint32(250000),
        + 		"current_level_progress_x1000": uint32(750000),
          		"given_amount":                 uint64(150),   ← 一致
          		"level":                        uint32(2),     ← 一致
          }
```

## 前提となる構成

- 変更の入り口が 3 層に分かれている: **契約（スキーマ/IDL のコメント・仕様書）→ 実装 → 外部から叩くテスト**。
- CI が `pull_request` イベントでのみ走り、**統合ブランチへの push では走らない**。
- ブランチ保護で「base が最新であること（up-to-date）」を要求していない／マージキューが無い。
  そのため、古い base のブランチをそのままマージできる。
- 結合テスト／E2E は重いので、PR 単位でしか回らない（＝マージ後の実際の統合状態は未検証）。
- 「PR は細かく分割する」という規約はあるが、**分割単位の条件（単独で検証可能か）が明文化されていない**。

## 根本原因

### 1. テストを含まない契約変更 PR は、CI にとって no-op である

契約の記述だけを変える PR は、CI から見れば「コメントが変わっただけ」で、
その意味変更に実装が従っているかを確かめる仕組みがどこにもない。

```
# 変更前（契約）
# フィールド P: 現在レベルの「進捗割合」。0 で開始し、次レベル到達で最大値。

# 変更後（契約）— 意味が反転している
# フィールド P: 次レベルまでの「残り割合」。レベルアップ直後は最大値で、到達で 0。
```

この 1 コミットがマージされた瞬間から、統合ブランチは
**「契約 = 残り割合／実装 = 進捗割合」という矛盾状態**になる。
矛盾はテストで固定されていないので、誰にも通知されない。

### 2. 意味の反転は「型」でも「コンパイル」でも捕まらない

値域も型も同じ（`uint32`、0〜最大値）で、変わったのは意味だけ。
静的検査は無力で、**振る舞いを固定するテストだけが差を検出できる**。
そのテストが同じ PR に無いなら、検出は将来の誰かに委ねられる。

### 3. 消費側テストは「そのブランチ内では一貫している」ため緑になる

テストを追加した PR は、古い base（旧契約 + 旧実装）で書かれていれば、
rebase して**契約だけ**新しくなっても、実装が旧仕様のままなので自分の CI は通り続ける。

| ブランチ | 契約 | 実装 | 期待値 | 自 CI |
|---|---|---|---|---|
| PR-A（契約のみ） | 新 | 旧 | （テスト無し） | 緑 |
| PR-B（実装追随） | 新 | 新 | 単体のみ新 | 緑 |
| PR-C（E2E 追加） | 新 | **旧** | **旧** | 緑 |
| 統合後 | 新 | 新 | **旧** | **赤（100%）** |

つまり「各 PR が緑」は「統合状態が緑」を一切保証しない。
そして統合状態を試す最初の実行者は、CI ではなく**ローカルで長時間ループを回している開発者**になる。

### 4. マージ順が最後の一押しになる

実装追随（PR-B）→ 消費側テスト（PR-C）の順にマージされると、
**古い期待値が最後に着地する**ため統合ブランチが壊れる。
逆順（PR-C → PR-B）なら PR-B のマージ直前の CI で気づける可能性があったが、
それは偶然に依存しており、設計上の防御ではない。

## なぜ「フレーク」に見えて、実はフレークでないか

- **見え方**: 7 周成功して 8 周目で落ちた ⇒ 蓄積劣化・並行干渉・時刻境界を疑いたくなる。
- **実際**: ループの途中で作業ツリーを更新（pull）しており、8 周目が**新しいコードでの最初の実行**だった。
  同一 HEAD で単体実行すれば毎回落ちる、完全に決定的な失敗。
- **紛らわしさを増す要素**: 上流で誰かが修正を入れると、次の pull で「勝手に直る」。
  再現しない失敗として片付けられ、原因（分割の粒度）は記録されずに残る。
- **集計上の害**: この種の失敗は「不安定なテスト」として数えられ、
  PR 分割の粒度が原因であることが指標に現れない。件数だけを測る運用では、
  細分化した側が得をし、後始末（1 行の追随 PR・壊れた統合ブランチ）は誰の指標にも載らない。

## 調査方法（一般化した手順）

1. **決定的かどうかを最初に切り分ける**。同じ HEAD で当該テストだけを数回単体実行する。
   毎回落ちるならフレークではない。ここを飛ばすと環境要因の調査に何時間も溶ける。
2. **作業ツリーの HEAD の変化とループ開始時刻を突き合わせる**。
   `git reflog --date=iso` の pull 時刻と、ループの各周の開始時刻を並べる。
   「落ち始めた周の直前に pull がある」なら、環境ではなくコードの変化が原因。
3. **差分の形から意味の反転を疑う**。期待値と実測値の**和が満点になる**、
   あるいは補数・逆数の関係にあるときは、値ではなく**定義**が変わっている。
4. **契約を第三の基準として三者比較する**。「期待値」対「実装」の 2 者で議論すると
   どちらが正しいか決まらない。スキーマ／仕様書の記述を見て、どちらが契約違反かを判定する。
5. **関係コミットの ancestry を確認する**。各 PR のブランチ head に相手のコミットが含まれていたかを
   `git merge-base --is-ancestor <commit> <branch-head>` で機械的に確かめる。
   「どちらも相手を含まない」なら、統合状態は未検証だったと確定できる。
6. **契約変更コミットの変更ファイル一覧を見る**。契約ファイル 1 本だけ（テスト 0 本）なら、
   その時点から矛盾状態が始まっていたと断定できる。矛盾の継続時間は
   契約変更のマージ時刻と実装追随のマージ時刻の差で測る。
7. **統合ブランチの CI 実行履歴を確認する**。push イベントの実行が無ければ、
   「壊れても自動検知されない」構造そのものが原因の一部である。

## 解決策

### 一次対策: PR の分割単位を「単独で検証可能か」で決める

分割の粒度は行数やファイル数ではなく、**その PR 単体で振る舞いが固定されるか**で決める。

- 既存の値の**意味・単位・値域・符号**を変える契約変更は、
  実装の追随と、その値を検証する既存テストの期待値更新を**同じ PR に含める**。
- 契約ファイルのみの PR が許されるのは、**意味を変えない**補足・整形・誤字修正に限る。
- 「実装 + それを固定するテスト」は常に同 PR。テストだけを後続 PR に回すと、
  その間は無検証区間になる。

### 二次対策: 依存関係のある PR はマージ前 rebase を強制する

分割せざるを得ない場合（レビュー容量など）は、依存順を守るだけでは不十分で、
**後続 PR を先行 PR に rebase してから CI を通す**ことを必須にする。
仕組みとしてはマージキュー、またはブランチ保護の「base が最新であること」を要求する設定。
「各 PR が緑」ではなく「**マージされる形の組み合わせが緑**」を要求条件にするのが要点。

### 三次対策: 統合ブランチへの push で CI を走らせる

`pull_request` だけでなく統合ブランチへの push でも同じジョブを実行し、
壊れた状態を人間の手作業（誰かのローカル実行）ではなく CI が最初に見つけるようにする。
これは設定数行で、すり抜けの検知時間を「数十分〜数時間」から「1 ビルド」に縮める。

### 四次対策: テストの期待値は契約から書く

期待値を実装の出力を見て埋めると、**実装のバグを仕様として固定**してしまう。
契約（スキーマのコメント・仕様書）を読んで期待値を決め、実装が食い違うなら
「テストの間違い」ではなく「実装の契約違反」として扱う。
契約が曖昧で決められないなら、それは契約を直す作業が先にあるというサイン。

### 五次対策: 指標に「後追い修正」と「統合ブランチ破損」を併記する

PR 件数のような分割を促す指標だけを見ていると、細分化の副作用が不可視になる。
後追い修正 PR 数・統合ブランチが赤だった時間・revert 数を併記し、
「件数が増えても後始末が増えていれば評価されない」形にする。

### 採ってはいけない対策

- **落ちたテストを quarantine／リトライで流す**。決定的な契約違反を隠し、
  誤った値のまま外部（クライアント実装など）へ露出する。
- **実装の出力を見て期待値を書き換える**。今回は結果的に正しい値と一致したが、
  それは契約を確認した結果でなければ「実装追随の追認」であり、次は逆向きに間違える。
- **「フレークだった」で記録を閉じる**。再現しない失敗として片付けると、
  同じ分割が繰り返され、同じ症状が別のテストで再発する。

## 一般化・チェックリスト

契約（意味）を変えるとき:

- [ ] この PR 単体で、変更した意味が**テストによって固定**されているか。
- [ ] 変更した値を参照している**未マージの PR / 既存テスト**を洗い出したか。
- [ ] 契約変更と実装追随が別 PR になる場合、**矛盾状態が続く時間**を意識し、最小化したか。
- [ ] 値の意味を反転させる変更（進捗↔残り、包含↔排他、開始↔終了、加算↔減算）で、
      型も値域も変わらないものは、静的検査が無力であると自覚しているか。

PR を分割するとき:

- [ ] 各 PR は単独で検証可能か（そのPR内のテストで振る舞いが固定されるか）。
- [ ] 依存のある PR を、先行 PR を取り込んだ状態で CI に通したか。
- [ ] レイヤー単位（契約だけ／実装だけ／テストだけ）の切り出しになっていないか。
      縦に薄く切る（小さくても検証可能な垂直スライス）方が常に安全。

テストが落ちたとき:

- [ ] 同一 HEAD で再実行して、決定的かフレークかを先に判定したか。
- [ ] 落ち始めた周の直前に、作業ツリーの更新（pull / rebase）が無いか。
- [ ] 期待値と実測値が補数・逆数の関係になっていないか（意味の反転のサイン）。
- [ ] 「期待値 vs 実装」の 2 者ではなく、契約を含めた 3 者で正しさを判定したか。

## 教訓

- **「各 PR が緑」は「統合状態が緑」を意味しない**。CI が検証しているのは各ブランチであり、
  マージ順に積み上がった実際の組み合わせは、誰も試していないことがある。
- **テストを含まない変更 PR は、CI にとって存在しない**。契約だけを変える PR は
  「レビューは通ったが検証はされていない」状態を作り、その負債を後続の誰かに渡す。
- **分割の正しい基準は大きさではなく検証可能性**。小さく割ること自体は善だが、
  検証不能な単位まで割ると、検証はマージ後（＝誰かのローカル）へ後回しになる。
- **決定的な失敗はフレークの顔をして現れる**。長時間ループのように「いつのコードを走らせているか」が
  曖昧な実行形態では、コードの変化を最初に確認しないと環境要因の調査に迷い込む。
- **意味の反転は最も静かな破壊**。型・値域・シグネチャが変わらないため、
  レビューでもコンパイラでも見落とされ、唯一の検出器が「契約から書かれたテスト」になる。

## 関連

- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) — テストは「バグの不在」を証明できない。
  本件は逆に、長時間ループが「統合状態が未検証である」ことを暴いた事例でもある。
- [テストランナーの結果キャッシュがヒットして「テストの副作用」が実行されない](./test-cache-hit-skips-server-startup-side-effect.md) —
  失敗の原因がテストコードの外（実行基盤・開発プロセス）にある型。`FAIL` 行の中身だけを見ても原因に届かない。
- [月単位の日付演算が正規化で繰り上がり、「前月」が「当月」になる](./month-arithmetic-normalization-overflow.md) —
  フレークの顔をして落ちるが実は決定的、という同じ構図。切り分けの第一歩が「決定性の確認」である点も共通。
