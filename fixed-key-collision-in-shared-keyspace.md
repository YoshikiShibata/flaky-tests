# 意味を持つ固定値が主キーのグローバルテーブルは、キー空間をテストごとに分割しないと衝突する

## 概要

主キーが「意味を持つ値」（発効時刻・バージョン・価格帯の下限・年など）であるグローバルなマスタテーブルに、複数の並列テストがそれぞれ前提データを seed する構成で発生する flaky。

テストデータのキーは通常 UUID やランダム値にして衝突を避けるが、この種のテーブルではキーの値そのものが検証の一部（「この時刻に発効した版が選ばれること」など）なので**ランダム化できない**。結果、実装者はキーをリテラルで書き、**兄弟テストからコピペした同じ値が複数のテストに現れる**。DB は全テストで共有されているため、同じ主キーへの seed が並列に走った瞬間に壊れる。

「テストごとに一意なキーしか INSERT しないなら Shared ロックで安全」（→ [全データ更新ジョブによるテスト間干渉](./global-data-modification-interference.md)）の前提が、**そもそもキーを一意にできない**ために成立しないケース。そしてこの型は、後述のとおり**ロックでは根治しない**。

## 症状

- 対象テストを単独実行すると常に成功する。並列実行のときだけ落ちる。
- 失敗はセットアップ段階で起き、主キー重複エラー（Spanner なら `AlreadyExists ... Row {<キー値>} already exists`）で即 `t.Fatalf`。**テスト本体のアサーションに到達しない**。
- エラーメッセージに出るのは重複した**キー値**だけで、「誰と衝突したか」は出ない。同じ値を使っている兄弟テストを自分で探す必要がある。
- 落ちるテストが実行ごとに入れ替わる（先に seed した側が勝ち、後から来た側が落ちる）。
- 稀に、主キー重複ではなく**アサーションの失敗**として現れる（後述の壊れ方 2）。こちらのほうが原因が遠い。

## 根本原因

### 問題のある構成

- テーブル V はグローバル（テナント／ユーザー等でスコープされない）で、主キーは意味を持つ単一値（例: 発効時刻）。
- セットアップヘルパーは `Insert`（`InsertOrUpdate` ではない）で行を作り、`t.Cleanup` で同じ行を `Delete` する。
- テスト A・B・C はいずれも `t.Parallel()` で、**同じキー値をリテラルで書いている**（コピペ由来）。

### 壊れ方 1: seed の主キー衝突

1. A が seed（行が存在するようになる）。
2. A が実行中（= まだ Cleanup していない）のうちに B が seed。
3. 主キー重複で B のセットアップが失敗。

窓は「A の seed から A の Cleanup まで」なので、A が短命なテストだと衝突確率は低く、**低頻度のフレーク**になる。

### 壊れ方 2: Cleanup による横取り削除

1. A が seed、B も（A の Cleanup 後に）seed 成功。
2. A が先に終了し、`t.Cleanup` が**同じ主キーの行を Delete**。
3. B はまだ実行中だが、B が前提としていた行が消えている。

`Insert` を `InsertOrUpdate` に変えると壊れ方 1 は消えるが、**壊れ方 2 は残る**。しかも壊れ方 2 はセットアップエラーではなくアサーション失敗として出るため、原因の特定が難しい。「重複を許して上書きする」方向の対策が不十分なのはこのため。

### なぜランダム化で回避できないか

通常のテストデータは ID を UUID にすれば衝突しない。しかしこの型のキーは値の**大小関係や絶対値が検証対象**になっている:

- 「参照時刻以前で最も新しい版が選ばれる」→ 版の発効時刻と参照時刻の関係が検証の本体
- 「この価格帯に属する選択肢だけが返る」→ 帯の境界値が検証の本体

そのためキーは固定値になり、固定値は**コピペで複製される**。「一意なキーを使えば安全」という一般則が、この構造では適用できない。

### なぜロックでは根治しないか

同じリソースに Exclusive ロックを取れば衝突自体は防げるが、

- **対象テストを全部直列化する**。共有したい状態は 1 つも無く、単に並列度を捨てるだけになる。
- 「該当する版が**存在しない**こと」を前提にするテストは救えない。これはテーブル全体の不在を主張するテストなので、並行テストの行は止められても seed／マイグレーション由来の行は消せない。結局キー空間の設計が必要になる。

つまりこの型の答えは排他制御ではなく**キー空間の分割**である。

## 一般化: この構造に該当するかの判定

以下が全て Yes なら、この型の衝突リスクがある。

1. そのテーブルはグローバルか（テスト固有の ID でスコープされないか）
2. 主キーの値は、テストが**意味を持って選んだ固定値**か（ランダム化すると検証が成立しないか）
3. その値をリテラルで書いているテストが 2 つ以上あるか
4. セットアップが `Insert` で、`t.Cleanup` が同じ行を `Delete` するか

該当するテーブルの例:

- 有効期間・発効時刻を主キーに持つ版管理マスタ（料金表・単価表・設定の世代管理）
- 帯・区間の境界値を主キーに持つ区分マスタ（価格帯・年齢区分・ランク境界）
- 年・月・日付を主キーに持つ期間マスタ（年度設定・キャンペーン期間）
- 「有効なものが唯一」という制約を持つマスタ（`is_active` 唯一行など）

## 解決策

### 1. キー空間の割り当てを 1 箇所に集約する

値をテストファイルに散在させず、「**どのテストがどの値を使うか**」の割り当て表を 1 箇所に置く。キーをテスト名にすると、後述の実行時検出が可能になる。

```go
// キーはトップレベルのテスト名。
var effectiveTimesByTest = map[string]time.Time{
    "TestFoo": time.Date(2020, 6, 15, 0, 0, 0, 0, time.UTC),
    "TestBar": time.Date(2020, 6, 16, 0, 0, 0, 0, time.UTC),
    ...
}
```

集約するだけでは「将来のテストが既存の値をコピペする」余地が残る。**値をテスト名からしか引けないようにする**と、使い回すには他テストの名前を騙るしかなくなる。

```go
func acquireKey(t *testing.T) time.Time {
    t.Helper()
    // サブテストから呼ばれてもトップレベル名で引く
    name, _, _ := strings.Cut(t.Name(), "/")
    v, ok := effectiveTimesByTest[name]
    if !ok {
        t.Fatalf("割り当て表に %s のエントリがありません。他テストの値を使い回さず固有の値を追加してください", name)
    }
    return v
}
```

### 2. seed をヘルパーに統一し、キーと参照時刻を同じソースから配る

この型のテストは「seed したキー」と「そのキーを引く時刻」が一致していることが安全性の根拠になる（一致していれば、他テストの版が同時に何本あっても自分の版が選ばれる）。両者を別々に書くと不変条件が壊れうるので、ヘルパーが seed して**使うべき値を返す**形にする。

```go
func setUpVersion(t *testing.T, ctx context.Context, client *Client, value int64) time.Time {
    t.Helper()
    key := acquireKey(t)
    setUpRecords(t, ctx, client, []Mutatable{&Version{Key: key, Value: value}})
    return key // 呼び出し側はこれをそのまま参照時刻に使う
}
```

副産物として、コピペされていた seed ブロックが 1 行になる。

### 3. 割り当て表の重複を機械的に検出する

集約しても、表の中で値を重複させてしまえば元の木阿弥。**通常のテスト**として検査する。

```go
func TestKeyAllocation(t *testing.T) {
    if len(effectiveTimesByTest) == 0 {
        t.Fatal("割り当て表が空です") // 空でも合格する穴を塞ぐ
    }
    owners := make(map[time.Time]string, len(effectiveTimesByTest))
    for name, v := range effectiveTimesByTest {
        // 比較は location を正規化した値で行う（後述）
        key := v.UTC()
        if owner, ok := owners[key]; ok {
            t.Errorf("%v が %s と %s で重複しています", key, owner, name)
        }
        owners[key] = name
    }
}
```

**時刻をキーにするときの落とし穴**が 2 つある。

- `time.Time` をそのまま map キーにすると比較に location が含まれる。DB 側の主キーは瞬間で一致するので、**別 location で書いた同一瞬間**（`2020-06-15 09:00 JST` と `2020-06-15 00:00 UTC`）は DB では衝突するのに検査をすり抜ける。`.UTC()` で正規化してから比較する。
- `UnixNano()` を使うと 1678〜2262 年の外で未定義（オーバーフロー）。sentinel に古い年を使うテーブルでは踏みうるので、キーには使わない。

**この検査は init 時の panic で実装しないこと。** 起動時 panic はテストフレームワークの外で落ちるため、無関係なテストだけを `-run` で流したい場合にもバイナリごと起動不能になり、失敗の報告経路も他のテスト失敗と別物になる。「programmer misuse は error / テスト失敗として扱う」という一般則に従う。ただし通常のテストにすると `-run` で絞った実行では走らないので、**フルスイートでのみ守られる**ことは自覚しておく。

### 4. 1 テストが 2 つのキーを同時に持つ誤用を検出する（任意）

割り当てが 1 テストに 1 つなので、**同じテストの並列サブテストがそれぞれ seed する**と、テスト間で潰したのと同じ壊れ方がテスト内で再発する。予約集合を持って検出できる。

```go
var seeding = struct {
    sync.Mutex
    names map[string]struct{}
}{names: map[string]struct{}{}}
```

acquire 時に登録し、`t.Cleanup` で解放する。**Cleanup の登録順が効く**: acquire を seed より先に呼べば、LIFO により「行の削除 → 予約の解放」の順になり、逐次に seed し直す使い方を妨げない。逆順にすると解放が先に走り、行がまだある状態で次の Insert が走って落ちる。

このガードの効果は限定的であることを正直に書いておくべき。**これが無くても壊れたデータは作られず、主キー重複で落ちるだけ**である。価値は「今まさに潰したフレークと見分けがつかないエラーメッセージ」を「原因を名指しした失敗」に置き換えることにある。

なお `t.Fatalf` はロック保持中に呼んでも安全（`runtime.Goexit` は defer を実行するので `defer Unlock()` が走る）。ただし `t.Cleanup` 登録**より前**に落とさないと、失敗した側が先行保持者の予約を消してしまう。

### 5. 「まだ何も無い」sentinel は、割り当て表の外から来る行まで疑う

「該当する版が存在しないとき」の振る舞いを検証するテストは、`t.Parallel()` な他テストのどの値よりも前（後）の sentinel 値を使う。ここで見落としやすいのが、**割り当て表の検査は表のエントリしか見られない**という点。seed ファイル・マイグレーション由来の行は検査対象外である。

sentinel を安全側（十分に外側）に取るだけでなく、**前提が成立していることを実行時に確認する**。

```go
func assertNoVersionAtOrBefore(t *testing.T, ctx context.Context, client *Client, threshold time.Time) {
    // 実装の解決規則が「<= 参照時刻」なら、検査も同値を含める（!After）
}
```

これが要る理由は、**前提が崩れたときの壊れ方が消費者ごとに非対称**だから。

- 「値の有無」をそのまま返す API を検証するテスト → 版が引けた瞬間に落ちる（loud）
- 「結果が作られないこと」を検証するテスト → 版が引けても**別の事由で**結果が作られないなら**緑のまま通る**（silent）。検証が黙って失われる

silent 側が現実的な経路かどうかは、実際に混入しうる行の値で決まる（例: マイグレーションが入れる初期版の単価が 0 なら、まさに silent 側を踏む）。**「落ちるから気づける」と決めつけず、消費者ごとに分岐を追うこと。**

### 6. Insert のままにするか InsertOrUpdate にするかは、どちらの誤りを loud にしたいかで決める

| | 中断された実行の残留行 | キー値をテスト間で共有してしまった場合 |
|---|---|---|
| `Insert` | そのテストは以後ずっと主キー重複で落ちる（自己回復しない） | 主キー重複で loud に落ちる |
| `InsertOrUpdate` | 上書きされて自己回復する | **黙って上書き**され、壊れ方 2 に化ける |

キーが固定値なので、残留行は自己回復しない点に注意（ランダムキーなら次回は別キーになるので自然に消える）。`Insert` を選ぶなら、**残留時の復旧手順（DB の作り直し）をヘルパーの近くに書いておく**。次の人が「今まさに直したフレークの再発」と誤診するのを防げる。

## 検出の手がかり

- セットアップのエラーが主キー重複で、**キー値がリテラルで書かれている**。そのリテラルを grep して 2 箇所以上に現れたら確定。
- 同じテーブルへの seed 箇所を全列挙し、キー値の一覧を作る。値が重複していないかを目で見る（これを機械化したものが上記 3）。
- 落ちるテストが実行ごとに入れ替わる（先着順で勝敗が決まるため）。
- CI では再現せずローカルでのみ再現する場合、**CI が直列実行になっていないか**を確認する（後述）。

## フレーク率の見積もりを間違えないこと

「N 回のループの N 回目で初めて落ちた」は「1/N の低頻度フレーク」を意味しない。**そのテストが何回実行されたか**を数える必要がある。

新しく追加されたテストは、ループの途中から実行対象に入る。ログ上でループが 60 周していても、当該テストの実行回数が 2 回なら**実質 1/2 で落ちている**高頻度フレークである。ログ内で当該テスト名の PASS/FAIL 行を数えて、周回数と一致するかを確認する。

（巨大なログを扱うときは、まず失敗行の位置を特定して周辺だけを抜き出す。全体を読もうとしない。）

## CI が直列実行だと、この欠陥は CI で一度も現れない

テストランナーを `-parallel=1` で走らせている場合、`t.Parallel()` なトップレベルテストは同時に走らないため、**この型の分離欠陥は CI では 100% 再現しない**。再現するのは `-parallel` を指定していないローカル実行（既定は `GOMAXPROCS`）だけになる。

これは「CI が緑だから分離できている」の反例であり、逆に「ローカルでだけ落ちるからローカル環境の問題」という誤診も招く。**分離欠陥は CI の設定に隠れているだけで実在する**。将来 `-parallel` を上げる判断のためにも、どの実行経路で再現したかを記録しておく。

## 修正の検証（変異テスト）

この型の対策は「壊れないこと」を確認しても意味がない（元々低頻度なので、通っただけでは対策の効果を証明できない）。**ガードが本当に発火するか**を、意図的に壊して確認する。

1. 割り当て表の 2 エントリを同じ値にする → 重複検出テストが「どのテスト同士か」を出して落ちること
2. location だけ違う同一瞬間を入れる → 同じく落ちること（`.UTC()` 正規化の確認）
3. 1 テストから 2 回 seed する → 二重保持ガードが落ちること
4. sentinel を既存の値より内側へ動かす → 前提チェックが落ちること

そのうえで、修正後のサブセットを連続ループで回して 0 FAIL を確認する。順序は「変異で発火を確認 → ループで安定を確認」であり、逆順だけでは空振り（ガードが何も検査していない）を見逃す。

## 教訓

1. **キーをランダム化できないテーブルがある。** 主キーの値そのものが検証の一部なら、UUID 化による衝突回避は使えない。代わりに**キー空間をテストごとに分割**し、割り当てを 1 箇所で管理する。
2. **固定値はコピペで複製される。** 兄弟テストから設定ブロックごとコピーするのが自然な書き方なので、「値が重複していないか」は人間の注意力ではなく機械で検査する。
3. **共有キー空間の壊れ方は 2 つある。** seed の衝突（loud）と、Cleanup による横取り削除（silent）。前者だけを見て `InsertOrUpdate` にすると後者が残り、しかも silent 側に寄せてしまう。
4. **答えがロックでない型がある。** 共有したい状態が無く、単に値がぶつかっているだけなら、ロックは並列度を捨てるだけで根治しない。「不在」を主張するテストはロックでは救えない、という切り分けも有用。
5. **「まだ何も無い」を前提にするテストは、前提を実行時に確認する。** その前提は割り当て表の外（seed・マイグレーション）から崩されうる。崩れたときに loud に落ちる消費者と silent に通る消費者が混在するので、**落ちる方だけを見て安心しない**。
6. **ガードは通常のテスト失敗として実装する。** 起動時 panic は `-run` での部分実行を不能にし、報告経路も他の失敗と揃わない。代わりにフルスイートでしか走らないことは自覚する。
7. **フレーク率は「テストの実行回数」で数える。** 周回数で割ると、新規追加テストの高頻度フレークを低頻度と誤認する。
8. **CI の直列実行は分離欠陥を隠す。** CI が緑であることは分離できている証明にならない。
9. **対策の検証は変異テストで行う。** 低頻度フレークは「通った」では効果を測れない。意図的に壊してガードの発火を確認する。

## 関連

- [全データ更新ジョブによるテスト間干渉](./global-data-modification-interference.md) — 「一意なキーのみ INSERT なら Shared で安全」の一般則。本ドキュメントはその前提（キーを一意にできる）が成立しないケース
- [ユニークキー INSERT でも「最新を選ぶグローバルセレクタ」を奪うと writer になる](./unique-insert-hijacks-global-latest-selector.md) — キーは一意だがグローバルな選択結果を動かす型。本件では「seed したキー == 引く時刻」を保つことで自分の版が必ず選ばれるため、セレクタ奪取は起きない（キーが重複していたことだけが問題だった）
- [テストデータのハードコード値とドメイン定数の不一致](./hardcoded-domain-values-in-test-data.md) — ハードコード値が「不正」で他テストを壊す型。本件はハードコード値が「衝突」する型
- [セットアップ・クリーンアップの順序が引き起こす孤立参照](./setup-cleanup-order-orphan-reference.md) — Cleanup の順序と LIFO が絡む型
- [共有フィクスチャプールの再利用による残留状態の干渉](./shared-fixture-pool-residual-state.md) — 共有リソースの残留状態が「初期状態」前提を壊す型
- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) — 低頻度フレークの発見手段
