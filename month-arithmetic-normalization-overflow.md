# 月単位の日付演算が正規化で繰り上がり、「前月」が「当月」になる

## 概要

「前月の月初日」を求めるテストの seed 計算を `now.AddDate(0, -1, 0)` で書いていたため、
**月末日に実行すると当月へ繰り上がり**、前月の値として仕込んだはずのデータが当月扱いになって
検証が壊れた。

`AddDate` は存在しない日付を正規化する（`6/31` → `7/1`）。したがって 7/31 に実行すると
`AddDate(0, -1, 0)` は 6 月ではなく **7 月** を返す。その後で日を 1 にしても当月の月初日に
なるだけで、「前月」にはならない。

間欠的に見えるが**フレークではない**。特定の日付なら 100%、それ以外の日付なら 0% で失敗する
決定的なバグで、年に 7 日だけ発火する。

## 症状

月次で積み上がる累計値（当月の消化額・利用量など）と上限値を比較し、超過なら
`FailedPrecondition` を返す API があるとする。E2E は「累計値の対象月が前月（＝月初リセットが
まだ適用されていない古い値）なら、当月の累計は 0 とみなすので、上限を下回る編集でも成功する」
ことを検証していた。

```
xxx_test.go:NN: status.Code(err) = FailedPrecondition, want OK
```

サーバ側の構造化ログ:

```
"attrs.limit":"300000","attrs.accumulated":"400000",
"error":"FailedPrecondition: ...: limit is below accumulated amount"
```

`accumulated` に、**前月値として seed した 400000 がそのまま乗っている**。つまり
「古い値なので 0 とみなす」経路に入っていない。

## 前提となる構成

サーバ側は「累計値の対象月」と `now` の月を比較して計上を決める。よくある実装:

```go
// 累計値は「対象月 == 当月」のときだけ計上する。
// 対象月が過去なら、月初リセットがまだ適用されていない古い値とみなして無視する。
func (r *Record) Accumulated(now time.Time) int64 {
    nowInZone := now.In(zone)
    target := r.TargetMonth.In(zone)

    if target.Year() == nowInZone.Year() && target.Month() == nowInZone.Month() {
        return r.Amount
    }
    return 0
}
```

テスト側は「前月の月初日」を作って seed していた:

```go
now := time.Now()

// Bad: 月末日に実行すると当月へ繰り上がる
prev := now.In(zone).AddDate(0, -1, 0)
targetMonth := time.Date(prev.Year(), prev.Month(), 1, 0, 0, 0, 0, zone)
```

## 根本原因

### 1. `AddDate` は存在しない日付を正規化する

Go の `time.Time.AddDate` は「年・月・日を加算してから正規化する」仕様で、はみ出した日は
翌月へ送られる。

```go
now := time.Date(2026, 7, 31, 0, 7, 36, 0, zone)
now.AddDate(0, -1, 0)   // 2026-06-31 → 正規化 → 2026-07-01
```

その後で日を 1 にしても、それは「繰り上がった後の月」の 1 日でしかない。

```
2026-07-31 → AddDate(0,-1,0) → 2026-07-01 → 日を1に → 2026-07-01  ← 当月！
2026-07-30 → AddDate(0,-1,0) → 2026-06-30 → 日を1に → 2026-06-01  ← 前月（期待どおり）
```

正方向も同じ罠がある: `2026-01-31` に `AddDate(0, 1, 0)` は 2 月ではなく **3/3** になる。

### 2. 判定が「年月の一致」なので、1 日のズレが結果を反転させる

seed が当月月初になると年月一致が成立し、無視されるはずの累計値が計上される。上限を下回る値で
編集する検証だったため、`OK` を期待していた呼び出しが `FailedPrecondition` に転ぶ。

### 3. 発火するのは年 7 日だけ

`AddDate(0, -1, 0)` が繰り上がるのは「前月の日数 < 当月の日（day-of-month）」のときだけ。

| 実行日 | 前月の日数 | 結果 |
|---|---|---|
| 3/29・3/30・3/31 | 2 月 = 28 日（閏年は 29 日） | 繰り上がる |
| 5/31 | 4 月 = 30 日 | 繰り上がる |
| 7/31 | 6 月 = 30 日 | 繰り上がる |
| 10/31 | 9 月 = 30 日 | 繰り上がる |
| 12/31 | 11 月 = 30 日 | 繰り上がる |
| 1/31・8/31 など | 前月 = 31 日 | 安全 |
| 上記以外の全日 | — | 安全 |

平年 7 日 / 閏年 6 日。毎日 CI を回していても月末しか出ないため、導入から**約 1 か月潜伏**した
（実例では導入が月初、初発火がその月の 31 日）。

## なぜ「フレーク」に見えて、実はフレークでないか

長時間の連続ループ実行（同じスイートを 10 回繰り返す等）で、**特定の 1 回だけ落ちた**という
現れ方をする。しかしこれは並行性・負荷・エミュレータ劣化ではなく、**その回だけ日付が変わって
いた**だけである。実例では 1〜8 回目が 30 日、9 回目が日付を跨いで 31 日の 00:00 開始だった。

「N 回目だけ落ちた」＝「累積劣化」と早合点しやすいポイント。**まず各回の開始時刻（実時刻）を
並べる**こと。

## 調査方法（一般化した手順）

1. **サーバのエラーログで、期待と実際のどちらの分岐に入ったかを確定する**
   今回は比較に使った両辺（上限と累計）がログに乗っており、累計に前月値が入っていることで
   「古い値を無視する経路に入っていない」と即断できた。ここで初めて「テストの seed が当月に
   なっている」という仮説に到達する。
2. **各ループの開始時刻を突き合わせる**
   ループのカウンタ／タイムスタンプログを見て、落ちた回だけ日付が違わないかを確認する。
   日付依存が疑えれば、環境要因（時計スキュー・エミュレータ劣化）を掘る無駄を省ける。
3. **日付演算だけを切り出して再現する**
   本体を動かさずに、失敗日の時刻を直接入れて確認する。数秒で確定できる。

```go
for _, d := range []time.Time{
    time.Date(2026, 7, 31, 0, 7, 0, 0, zone),
    time.Date(2026, 7, 30, 20, 58, 0, 0, zone),
} {
    fmt.Println(d.Format("01-02"), "->", d.AddDate(0, -1, 0).Format("2006-01-02"))
}
// 07-31 -> 2026-07-01   ← バグ
// 07-30 -> 2026-06-30
```

4. **修正前の形に戻して失敗を再現し、「空振りしていない」ことを確認する**
   修正後に PASS しても、それが「修正のおかげ」か「今日はたまたま安全な日」かは区別できない。
   **発火日のうちに**、修正を一時的に取り消して（誤った日付計算に戻して）同じ失敗が出ることを見る。
   発火日を過ぎてしまった場合は、サーバ時刻を注入できる仕組みで月末日を与えて再現する。

## 解決策

### 一次対策: 月初へ丸めてから月を加減算する

日が 1 なら、どの月へ移動しても存在するので正規化が起きない。

```go
// Good: 月初へ丸めてから 1 か月引く
func previousMonthStart(t time.Time) time.Time {
    v := t.In(zone)
    return time.Date(v.Year(), v.Month(), 1, 0, 0, 0, 0, zone).AddDate(0, -1, 0)
}
```

「月末日」が欲しい場合も同じ発想で、月初を起点に `AddDate(0, 1, -1)`（翌月 1 日の前日）として
求める。**日を保ったまま月を動かさない**のが要点。

### 二次対策: 月・日の判定に依存するテストはサーバ時刻を固定する

一次対策で日付演算は正しくなるが、**seed の基準時刻はテストプロセスの `time.Now()`、判定は
サーバの `now`** という非対称が残る。両者が月境界を挟んでズレると、同じ症状が再発する。

```
テスト now = 8/01 00:00:00.05  → seed する対象月 = 7 月（前月のつもり）
サーバ now = 7/31 23:59:59.9   → サーバの当月 = 7 月
→ 「前月として seed した値」が当月と一致してしまう
```

数百 ms のズレでも起きる。テストプロセスとサーバが別ホスト／別 VM で動く構成では、長時間ループ中
に一方の時計が遅れる事象も実際に踏んでいる。したがって、**時刻に依存する判定を検証するテストは、
リクエスト単位でサーバの `now` を注入して固定する**。

```go
// テスト用の時刻注入（リクエスト単位でサーバの now を固定する仕組み）
ctx = withFixedNow(ctx, now)
```

同一ファイル内の類似テストが既にこれをやっている場合、それは「先に踏んだ人が付けた保険」で
あることが多い。**同型のテストで扱いを揃える**。

### 採ってはいけない対策

- **月末を避けて実行する**: 発火日を隠すだけで、その日付で通る保証は得られない
- **落ちた回をリトライする**: 決定的バグなので同じ日なら必ず落ちる。リトライは無駄

## 一般化・チェックリスト

- [ ] `AddDate(0, ±N, 0)` の起点が「日が 1 に丸められている」ことを確認したか（丸めていないなら
      月末で必ずズレる）
- [ ] 「前月」「翌月」「N か月前」を求めるコードが、月末日を起点に呼ばれる可能性はないか
- [ ] 日を保ったまま月を動かす意図なのか、月初／月末が欲しいのかを区別しているか
- [ ] 日付演算のタイムゾーンは、判定側（サーバ）と同じか
- [ ] 月・日の一致判定を検証するテストで、seed の基準時刻と判定側の `now` が同一だと保証されて
      いるか（保証できないなら時刻注入で固定する）
- [ ] 「N 回目だけ落ちた」を累積劣化と決めつける前に、各回の開始日時を並べたか
- [ ] 修正が効いていることを、発火日のうちに「修正を一時的に取り消すと同じ失敗が再現する」ことで確認したか

## 教訓

1. **`AddDate` の正規化は「エラーにならない」ため見逃す**。存在しない日付は例外ではなく静かに
   繰り上がる。月単位の演算は必ず「日を 1 に丸めてから」行う
2. **決定的な日付バグは、フレークの顔をしてやってくる**。連続ループの 1 回だけ落ちたときは、
   まず各回の実行日時を確認する。負荷・並行性・エミュレータを疑うのは後回し
3. **導入から発火までの潜伏期間が長い**。年 7 日しか出ないので、CI が毎日緑でも「日付演算が
   正しい」証拠にはならない。レビュー時点で気づくしかない類のバグ
4. **サーバのエラーログに、比較に使った両辺の値が乗っていると切り分けが一撃で終わる**。
   今回は累計値に前月値が入っている 1 行で、環境要因の線が消えた
5. **修正が通ったことと、修正が効いたことは別**。発火条件を満たしている間に、修正を一時的に
   取り消して失敗を再現し、テストが空振りでないことを確かめる

## 関連

- [月末境界による Flaky Test：月別設定値の切り替わり](./month_boundary_master_data_switch.md) —
  同じく「月末（31 日）に失敗」だが、機構は「テスト内で時間を進めて月をまたぐ」もの。
  対策の「基準時刻を月初に固定する」は本件の一次対策と同じ発想
- [時刻境界で失敗するフレーキーテストの原因と対策](./flaky-test-time-boundary-patterns.md) —
  テストとサーバで `time.Now()` の呼び出しタイミングがズレる型（本件の二次対策の一般形）
- [業務時間境界をまたぐテストデータによるフレーキーテスト](./time-frame-boundary-crossing.md) —
  境界の「位置」がドメイン固有な場合
- [現在時刻から導出される値のハードコードによる Flaky Test](./hardcoded-season-from-current-time.md) —
  現在時刻から導出される値を期待値に固定してしまう型
- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) —
  年 7 日しか出ない欠陥は、回数ではなく「いつ回すか」でしか捕まらない
