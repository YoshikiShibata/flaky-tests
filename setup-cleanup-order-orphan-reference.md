# セットアップ・クリーンアップの順序が引き起こす孤立参照による Flaky Test

## 概要

テストヘルパーが「子レコードを先に INSERT し、親レコードを後で INSERT する」順序になっていると、`t.Cleanup` の LIFO により setup 中・cleanup 中の双方で「子レコードは存在するが、参照先の親レコードが存在しない」状態が一瞬発生する。

並列実行される別テストが、この一瞬の不整合状態でテーブルをグローバルに走査し、子レコードの参照を辿るような処理を実行すると、参照先未存在エラーで失敗する。

不整合の向きは 2 通りある。**「子が残って親が無い」だけでなく、「親が残って子が無い」でも同じことが起きる。** 後者は挿入順序ではなく削除順序に起因し、後述の「[逆向きのケース: 親が残り、子が消える](#逆向きのケース-親が残り子が消える)」で扱う。前者の解決策（親を先に挿入する）は後者には適用できないため、対策が異なる。

## 発生条件

以下の3条件が揃うと発生する:

1. **参照関係を持つ複数レコードのセットアップ** — `Child.ParentID` のような参照を持つレコード群を別々の `SetUp*` ヘルパーで作成する
2. **挿入順序が参照方向と逆** — Child → Parent の順で挿入している（あるいは ParentID を Child に設定した上で Parent を後から作る）
3. **並列実行される別テストがグローバル走査と参照解決を行う** — 例: ジョブテストがテーブル全行を走査し、各行の参照先を別テーブルから取得する

## なぜ問題が起きるか

### Setup 中の不整合状態

```go
// 旧コード: Child を先に挿入
bsp := db.SetUpChild(t, func(c *Child) {
    c.ParentID = uuid.NewString() // まだ存在しない Parent.ID
})

db.SetUpParent(t, func(p *Parent) {
    p.ID = bsp.ParentID
})
```

`SetUpChild` 完了から `SetUpParent` 完了までの間、Child は存在するが対応 Parent が存在しない。

### Cleanup 中の不整合状態（LIFO）

`t.Cleanup` は LIFO で実行されるため、cleanup 順序は setup の逆になる:

```
setup:   Child → Parent
cleanup: Parent → Child  ← Parent 削除後、Child が孤立
```

`Parent.Delete` 実行後 `Child.Delete` 実行前の間、Child は存在するが Parent は存在しない。

### 並列テストの干渉

ジョブテストなどが「テーブルを全行走査して各行の参照先を取得する」処理を実行する場合、上記の不整合期間中に処理が走ると参照先未存在エラーで失敗する。

```
TestA setup:  Child INSERT → (この間に TestB がジョブ実行) → Parent INSERT
                                  ↓
                            TestB が Child を見つけ Parent を探すが見つからない → fail
```

## 解決策

### 挿入順序を参照方向に合わせる

Parent を先に作成し、その ID を使って Child を作成する:

```go
// 修正後: Parent を先に挿入
parent := db.SetUpParent(t, func(p *Parent) {
    p.SomeField = "..."
})

db.SetUpChild(t, func(c *Child) {
    c.ParentID = parent.ID
})
```

これにより:

- **Setup 中**: Parent 挿入完了 → Child 挿入完了。Child が存在するときは必ず Parent も存在する
- **Cleanup 中（LIFO）**: Child 削除 → Parent 削除。Parent が存在する間は Child が孤立しない

setup・cleanup のどの瞬間においても、Child が存在する間は対応 Parent が存在することが保証される。

## 逆向きのケース: 親が残り、子が消える

### 何が起きるか

後片付けで「1 エンティティに紐づく複数テーブルの行をまとめて削除する」ヘルパーは、参照整合性制約（外部キー）に従って **子テーブル → 親テーブル** の順に削除しなければならない。その結果、親行の削除が最後になり、しかもそれが子削除とは別のコミットになっていると、後片付けの途中に

> **親行はまだ存在し、しかも「走査対象になる述語」を満たしたままなのに、その子行は既に消えている**

という中間状態が生まれる。ここに並列実行中の別テストのグローバル走査が入ると、走査側は「親を見つけたのに子が引けない」状態を観測する。

走査側の実装が「親があるなら子は必ずある」を不変条件として扱い、破れたときにエラー（「あり得ない状態」「バグの可能性あり」といった内部エラー）を返す設計だと、**後片付けとは無関係なテストが落ちる**。走査側が自分の作ったデータしか検証していなくても、走査そのものがグローバルなので巻き込まれる。

```
TestA 後片付け: 子行 DELETE ─────────────→ 親行 DELETE
                              ↑
                この間に TestB がグローバル走査
                → 親を拾う → 子が無い → 内部エラーで TestB が fail
```

### 発生条件

1. **後片付けが子 → 親の順に削除する** — 参照整合性制約があるので順序を逆にはできない
2. **親行の削除が子削除と別コミットになっている** — 例: 子は一括 DML、親行は別の書き込み API で削除
3. **親行が走査の述語を満たしたまま残る** — 走査が「状態列が有効」「更新時刻が期間内」などで絞る場合、削除途中の親行はその条件をまだ満たしている
4. **並列実行される別テストが、親をグローバル走査して子を必須参照する処理を回す** — 子が無いことをエラーにする実装だと即失敗する

### 「親を先に」は使えない

前半のケースの解決策（参照される側を先に作り、LIFO で子を先に消す）は、**削除側には適用できない**。参照整合性制約により親行を先に削除できないためで、削除順序は常に子 → 親に固定される。よって別の手を打つ必要がある。

### 対策

優先順に 3 つある。

**(a) 削除の前に、親行を走査の述語から外す**

親行を消せないなら、走査の絞り込み条件に合致しない状態へ先に落とす（状態列を「無効」「終了」相当の値へ更新する等）。重要なのは **その更新を子削除と同一トランザクションでコミットする**こと。同一トランザクションなら「述語を満たすのに子が無い」状態がコミット済みの状態として他トランザクションから読まれることはない。一括 DML を 1 トランザクションで実行する構成なら、先頭に 1 文足すだけで済み、往復も増えない。

順次コミットになるエンジン構成では原子性は得られないが、「述語から外す更新が子削除より先にコミットされる」順序は保たれるので、窓は縮む。

**(b) 走査側が複数スナップショットで読むなら、排他制御を足す**

(a) だけでは塞げない経路が残る。走査側が「親の一覧取得」と「子の取得」を**別々のスナップショット**で読む実装だと、

```
走査: 親一覧を読む（この時点では親も子も健全）
                    ↓ この隙に後片付けがコミット
走査: 子を読む → 無い → 内部エラー
```

という順序が成立し得る。窓の幅は「後片付けの全区間」から「走査側の 2 回の読み取りの間」まで縮むが、ゼロにはならない。ここを保証したければ、走査を回すテストと、走査対象になるデータを作って消すテストを**排他ロックで直列化する**。ロックは「データ作成 → 走査 → 後片付け」の全区間で保持する必要がある（`t.Cleanup` で解放するロックなら、後片付けの `t.Cleanup` 登録より前に取得すれば LIFO で後片付け完了後に解放される）。

**(c) 可能なら後片付け全体を 1 トランザクションにする**

親行の削除も同じトランザクションに入れられるなら、中間状態そのものが存在しなくなり (a) の更新も不要になる。ただしエンジン構成や書き込み API の制約で常に選べるとは限らない。

### 対策の役割分担

(a) と (b) は代替ではなく補完関係にある。

| 対策 | 効く範囲 | 限界 |
|---|---|---|
| (a) 述語から外す | 後片付けの全区間で観測される中間状態を消す。ロックの規約を知らないテストにも効く | 走査側が複数スナップショットで読む隙は残る |
| (b) 排他ロック | 走査と後片付けが重なる経路自体を無くす | 対象テストがロック規約を守る前提。守る範囲を全数調査して文書化しないと将来漏れる |

(a) だけで済ませたくなるが、それは窓を狭めるだけであって保証ではない。逆に (b) だけにすると、規約を知らずに書かれた将来のテストで再発する。両方入れるのが安全側で、**「(a) があるから (b) は不要」と判断されないようコード側のコメントに残る経路と担保先を明記しておく**。

### 検出方法

- 内部エラーに現れるエンティティ ID が、落ちたテストが作ったものかを確認する。**無関係な ID なら他テストの中間状態を踏んでいる**
- 失敗時刻が、他テストの後片付け区間に入っていないかをログのタイムラインで確認する。並列テストの開始・終了ログと突き合わせると、加害テストを特定できることが多い
- 走査の述語（状態列・時刻範囲など）を満たすデータを作るテストを全数洗い出す。そのうち **後片付けでそのデータを削除するテスト**だけが加害者になる。作るだけで消さないテストは中間状態を作らないので無害

## 一般化したパターン

| 項目 | 内容 |
|------|------|
| 不整合の種類 | 参照先未存在（外部キー制約相当の整合性違反） |
| 発生タイミング | Setup の中間・Cleanup の中間 |
| 発見の難しさ | 単独実行・小規模並列では再現しにくい。並列実行する別テストがグローバル走査するときだけ発生する |
| 検出方法 | 参照先未存在を示すエラーが、検証対象のレコード ID と無関係な ID で発生していないか確認する |
| 解決方法 | 参照される側（親）を先に挿入する。`t.Cleanup` の LIFO 性質を利用して cleanup 中も整合性を保つ |

### 2 つの向きの対比

| | 子が残って親が無い | 親が残って子が無い |
|---|---|---|
| 起因 | **挿入**順序が参照方向と逆（子 → 親） | **削除**順序が参照整合性に従う（子 → 親）ため親行の削除が最後の別コミットになる |
| 中間状態 | 子行はあるが参照先の親行が無い | 親行が走査の述語を満たしたまま残り、その子行が無い |
| 走査側の失敗の形 | 子を起点に親を引けない | 親を起点に子を引けない（「あり得ない状態」として内部エラーになる実装だと即失敗） |
| 解決策 | 親を先に挿入する（LIFO で子が先に消える） | 親を先に削除できないので不可。削除前に親を走査の述語から外す（同一トランザクションで）＋ 走査と後片付けを排他する |

## チェックリスト

参照関係を持つレコードを複数の `SetUp*` ヘルパーで作成するテストでは、以下を確認する:

1. **挿入順序は参照方向と一致しているか？** — `Child.ParentID = parent.ID` のような参照を作るなら、Parent を先に挿入する
2. **`t.Cleanup` の LIFO 順序を考慮しているか？** — 挿入順序を逆にしたものが cleanup 順序になる。cleanup 中も参照整合性を保てるか
3. **同じパッケージ内に並列実行される別テストはあるか？** — 特にグローバル走査するジョブテスト等
4. **そのジョブテストはテーブルを全行走査し、参照先を取得するか？** — する場合、不整合期間に干渉する

後片付けで「1 エンティティに紐づく複数テーブルの行をまとめて削除する」共有ヘルパーを使っている場合は、さらに以下を確認する:

5. **親行の削除は子削除と同一コミットか？** — 別コミットなら、その間に「親は走査の述語を満たすが子が無い」中間状態が観測される
6. **削除する前に、親行を走査の述語から外しているか？** — 状態列などで絞る走査があるなら、削除に先立って述語外の値へ落とし、子削除と同一トランザクションでコミットする
7. **走査側は親一覧と子を同一スナップショットで読むか？** — 別スナップショットなら (6) では塞がらない隙が残るので、走査を回すテストと排他する
8. **排他ロックは後片付け完了まで保持されるか？** — データ作成から後片付けまでを覆っていなければ、削除だけがロック外に出て意味を失う

## 教訓

1. **`t.Cleanup` は LIFO** — setup 順序と cleanup 順序は逆になる。両方の中間状態を考慮する
2. **「全レコード setup 完了後に検証」だけを考えると見落とす** — setup の途中・cleanup の途中も他テストから観測される
3. **グローバル走査するジョブとの組み合わせが特に危険** — テストが自分の作ったレコードしか触らなくても、他テストの中間状態を踏み抜く可能性がある
4. **並列テストパッケージ内のセットアップヘルパーは「常に参照整合性を保つ順序」で書く** — これは実コードのトランザクション順序設計と同じ原則
5. **不整合は「子が残る」向きだけではない** — 削除は参照整合性に従って子から消すしかないので、親が残る向きは順序の入れ替えでは直せない。**消せないなら走査の述語から外す**（状態列を落とす）という別の手が必要になる
6. **原子性を確保しても、走査側が複数スナップショットで読むなら保証にならない** — 「一覧を読む」「参照先を読む」が別スナップショットなら、その隙に後片付けがコミットされ得る。原子化は窓を狭める手段であって排他の代わりにはならない
7. **加害者になるのは「作って、かつ消す」テストだけ** — 走査の述語に合致するデータを作るテストを洗い出したうえで、後片付けで消すものだけが対象。作って残すテストは中間状態を作らないので無害。この線引きをせずにロックを広げると不要な直列化でテスト時間を損なう

## 関連パターン

- [全データ更新ジョブによるテスト間干渉](./global-data-modification-interference.md) — グローバル走査ジョブが他テストデータを上書きするパターン。本パターンは「上書き」ではなく「参照解決失敗」だが、グローバル走査と並列テストの組み合わせという点は共通
- [逐次ループの並行化におけるループキャリー依存性の見落とし](./loop-carried-dependency-in-concurrent-test-setup.md) — 並列化により暗黙の順序依存が壊れるパターン
- [グローバル集計値の before/after 比較が並行書き込みで揺れる](./global-aggregate-before-after-assertion.md) — 「2 つのスナップショットの間に他テストがコミットする」という構造は「親が残って子が無い」ケースの残余リスクと同じ。あちらは件数のズレ、こちらは参照解決失敗として表面化する
- [ユニークキー INSERT でも「最新を選ぶグローバルセレクタ」を奪うと writer になる](./unique-insert-hijacks-global-latest-selector.md) — 「自分専用の行しか触らないつもりでも、グローバルな選択・走査の結果を変えてしまう」という点が共通。排他が必要かどうかの判断軸として併読するとよい
