# 壁時計の後方ステップ（NTP補正）による順序依存テストのフレーキー失敗

## TL;DR

- `ORDER BY <作成時刻> DESC` でソートする一覧APIのE2Eテストが、**隣接2件だけ綺麗に入れ替わって**落ちることがある。
- 真因はプロダクトバグでもテストロジックの誤りでもなく、**テスト実行中に macOS の `timed`（時刻同期デーモン）が NTP 補正でシステム壁時計を後方へステップさせた**こと。
- 作成時刻を `time.Now()` で設定している場合、壁時計が巻き戻ると「後に作成したレコードの方が作成時刻が小さい」逆転が起き、DBのソート結果がテストの「作成順」前提と食い違う。
- macOS なら **統合ログ（`log show`）の `timed` プロセスに `settimeofday` / `adjust,-N` の痕跡が残る**ので、後追いで確定できる。
- 発症形は**3つ**ある。①隣接2件のスワップ（§4）②レコードが丸ごと欠落（§10）③「最新の1件」が別レコードになる（§11）。
  **1回のステップが、機序の違う無関係な複数テストを同時に壊す**ことがある（§4 末尾）。

---

## 1. 発生した問題（症状）

「特定の条件で絞り込んだレコード一覧を、作成時刻の降順で返す」一覧取得APIのE2Eテストで、
**隣接する2件のレコードがちょうど入れ替わった**形でアサーション失敗が出た。

```
index p   : got = レコードA, want = レコードB
index p+1 : got = レコードB, want = レコードA
```

入れ替わった2件は、識別子と一部の付随情報（割引の有無など）以外のフィールド
（所有者・ステータス・付与値など）は一致しており、**純粋に並び順だけ**が逆だった。

> 「隣接2要素の綺麗なスワップ」は、ソートの主キーが**同値（タイ）か逆転**したときの典型的なシグネチャ。
> まずここに着目すると原因の当たりが付けやすい。

---

## 2. 調査ステップ1：サーバ側は決定的か

並び順を疑う前に、サーバ側がそもそも非決定的でないかを確認する。

- 一覧クエリの `ORDER BY` を確認 → `作成時刻 DESC, 識別子 DESC` のように**完全に決定的**だった。
- レスポンス組み立ても、DBから取得したスライスの順序をそのまま保持して詰めるだけ。
  goroutine による並べ替えや map 経由の順序喪失もなし。

```sql
-- イメージ（一般化）
SELECT ... FROM <テーブル>
WHERE <絞り込み条件>
ORDER BY <作成時刻> DESC, <識別子> DESC
LIMIT @limit;
```

> **結論**: got（レスポンス）の順序は `作成時刻 DESC, 識別子 DESC` で決定的。
> `got = [A, B]` は `作成時刻(A) ≥ 作成時刻(B)` を意味する。

---

## 3. 調査ステップ2：期待値（want）の構築と矛盾

テストは、レコードを逐次作成しながら毎回 prepend して降順の期待配列を組んでいた。

```go
// 新しく作ったレコードを先頭に積む（=降順）
want = append([]*Record{newRecord}, want...)
```

作成時刻はレコード保存時に `time.Now()` で設定される（イメージ）:

```go
func setTimestamp(r *Record) {
    now := time.Now()
    if r.CreateTime.IsZero() {
        r.CreateTime = now
    }
    r.UpdateTime = now
}
```

作成ループは**完全に逐次**（各レコードの作成・確定処理はすべてブロッキング、goroutine なし）。
よって本来は次が成り立つはず:

```
逐次の実行順 == time.Now() の単調増加 == 作成時刻の昇順
⟹ want（実行順の逆＝最新が先頭） == got（作成時刻 DESC）
⟹ 一致するはずで、失敗しない
```

ところが実際は:

- `want = [B, A]`（B が先頭=index 小）
  ⟹ B の方が**後の反復で prepend された＝後に実行された**
- `got = [A, B]`
  ⟹ `作成時刻(A) ≥ 作成時刻(B)`

**つまり「後に実行した B の作成時刻の方が小さい」**。
逐次実行で `time.Now()` が単調増加なら絶対に起こらない。
→ **壁時計が単調増加していない**ことが論理的に確定する。

---

## 4. 調査ステップ3：実行ログのタイムスタンプ逆行

サーバの実行ログ（各行が unix エポックのタイムスタンプを持つ。作成時刻を刻むのと同じプロセスの時計）を
**出力順**に並べると、時刻が逆行していた。

```
（先に実行されたレコードの処理）  ts 1779677692700
                                ts 1779677693370
（次に実行されたレコードの作成）  ts 1779677691983   ← 約1.4秒 巻き戻り！
                                ts 1779677692068
                                ts 1779677693399   ← 元に戻る（前進）
```

- **実行順（=ログ出力順）**: A→B ではなく、先に処理されたのは「want で後ろに来るべき」レコード。`want = [B, A]` と整合。
- **作成時刻（壁時計）**: 先に実行した方が大きい値、後に実行した方が小さい値（巻き戻り後）。`got = [A, B]` と整合。

ここまでで「壁時計が後方へジャンプした」とほぼ断定できるが、OS のログで裏付けると確実。

### 長時間ループの巨大ログから失敗イテレーションを切り出す

**OS の統合ログは約半日でローテーションする**（§8 参照）。数十時間回すループでは、失敗時刻の `timed` ログが
すでに流れていて §5 の手段が使えないことが多い。その場合 **アプリ側の実行ログが一次証拠**になる。
（今回の 2026-08-02 のケースは失敗の約4.5時間後に調査したため、辛うじて両方が取れた。）

ループ全体のログは巨大になりがち（実測: 114イテレーションで **17GB / 1300万行**）なので、先に
**失敗したイテレーションだけを切り出す**と解析が一気に速くなる。ループがイテレーション境界を
ログに刻んでいるなら、それを使う:

```bash
# 1. 境界マーカの行番号を得る
grep -n "LOOP COUNT" result.txt | tail -5
#    13120947:START LOOP COUNT:  114

# 2. 失敗イテレーションだけを別ファイルへ（17GB/1300万行 → 145MB/9.3万行）
sed -n '13120947,13213813p' result.txt > run114.txt
```

切り出したら、**JSON ログの `timestamp` を出力順に走査**して閾値を超える逆行を探す:

```bash
grep -h "^{" run114.txt | python3 -c '
import sys, json, datetime
def f(ms): return datetime.datetime.fromtimestamp(ms/1000).strftime("%H:%M:%S.%f")[:-3]
prev = None
for raw in sys.stdin:
    try: d = json.loads(raw)
    except Exception: continue
    ts = d.get("timestamp")
    if ts is None: continue
    if prev is not None and ts < prev - 500:      # 0.5秒を超える逆行のみ
        print(f(prev), "->", f(ts), " back", round((prev-ts)/1000, 2), "s |", d.get("logger"))
    prev = ts
'
# => 09:37:58.757 -> 09:37:56.687  back 2.07 s | grpc.customer_usecase
```

**偽陽性に注意（閾値が要る理由）**:
テストプロセス自身の `log.Printf` 由来の行（ロック取得ログ等）は、**タイムスタンプを取ってから出力ロックを
取るまでの間に別 goroutine が割り込む**ため、正常時でも1秒程度の「逆行」が出る（実測で2件）。
判定は **閾値（>0.5秒）** を設けたうえで、**単一プロセスのサービスログ**で行うのが確実。
逆に、複数サービスのログが1本のパイプに混ざっている区間では、行の並び自体が時刻順とは限らない点にも注意。

### 複数テストが同時に落ちたら、まず1回のクロックステップを疑う

後退ステップは**プロセス全体の時計**を動かすので、その瞬間に走っていたテストすべてが影響を受けうる。
実際 2026-08-02 のケースでは、**1回の −2.073 秒ステップ**が、別スイート・**別機序**の2テストを
同一イテレーションで壊した（§10 の「丸ごと欠落」型と §11 の「最新1件が別レコード」型）。

> **診断の指針**: 長時間ループで、普段落ちないテストが**同一イテレーションでだけ複数落ちた**ら、
> それぞれの原因を個別に追う前に、まず**単一のクロックステップ**を疑って時刻の逆行を探す。
> 機序が違っても真因は1つ、ということがある。

---

## 5. macOS での「壁時計巻き戻し」ログ調査方法

macOS は統合ログ（unified logging）に時刻同期デーモン `timed` の挙動を残している。
`log show` で後追いできる。**バッファ保持期間内（ログ量により数時間〜数日）なら過去分も見える。**

### 手順 5-1. 問題のタイムスタンプをローカル時刻へ変換

ログのタイムスタンプ（ミリ秒なら秒に直す）を `date -r` で変換し、調査窓を決める。

```bash
# 例: 1779677693370(ms) → 1779677693(s)
date -r 1779677693   # => 2026年 5月25日 11時54分53秒 JST
date -r 1779677691   # => 2026年 5月25日 11時54分51秒 JST
```

→ 巻き戻りは **2026-05-25 11:54:51〜53 JST** 付近、と窓が絞れる。

### 手順 5-2. `timed` のログを窓で抽出

```bash
log show --start "2026-05-25 11:53:00" --end "2026-05-25 11:56:30" \
  --predicate 'process == "timed"' --info --debug \
  | grep -Ei 'settimeofday|Setting system time|adjust,-|RejectATime|Received time'
```

> 補足:
> - `--info --debug` を付けないと `timed` の詳細ログは出ない。
> - zsh ではコマンドを1行で書く（複数行＋バックスラッシュ継続でクォートが壊れ `too many arguments` になることがある）。
>   `log` が別物に解決される環境では `/usr/bin/log` を明示する。
> - 過去ログが流れていて見えない場合はリアルタイム監視に切り替える:
>   `log stream --predicate 'process == "timed"' --info --debug`

### 手順 5-3. 出力の読み方（実際に取れた証拠）

```
11:54:51.576109  timed: [com.apple.settimeofday:] settimeofday({0x6a13b9fb,0x8b87b}) == 0
11:54:51.576208  timed: Setting system time to Mon May 25 11:54:51 2026 from TMTimeSynthesizer
11:54:51.603870  timed: cmd,apply,src,settimeofday,…,adjust,-2.044957995,success,1
11:54:53.613854  timed: TDTF,RejectATime,…,innv,-2.044978,…,source,NTP
11:54:53.612950  timed: Received time Mon May 25 11:54:51 2026±0.01 from "NTP"
```

| ログ | 意味 |
|---|---|
| `settimeofday({0x6a13b9fb,0x8b87b})` | **緩やかな slew ではなく時刻を一気に書き換えるハードなステップ**。`0x6a13b9fb` = `1779677691`（tv_sec）、`0x8b87b` = `571003`（tv_usec）→ 時計を `1779677691.571` に設定し直した |
| `adjust,-2.044957995` | この適用で壁時計が **約 −2.04 秒 後方へジャンプ**した |
| `innv,-2.044978` (innovation) | ローカル時計が NTP 比で約 +2.04 秒**進んでいた**と判定 → それを打ち消すため後方補正 |
| `Received time … from "NTP"` | NTP サーバから受信した正しい時刻 |

> macOS は小さなズレは slew（周波数を微調整して徐々に追従）で吸収するが、
> **閾値（おおむね数百ms〜秒オーダー）を超えると `settimeofday` で一気にステップ**する。
> 今回は約2秒ズレていたためステップになった。

### 手順 5-4.（任意）スリープ/復帰の確認

スリープ復帰でも壁時計がジャンプすることがある。電源管理ログで確認:

```bash
pmset -g log | grep -Ei 'Wake|Sleep|DarkWake' | tail -50
```

今回はスリープではなく NTP ステップが主因だった。

### 手順 5-5. step と slew の違い（なぜ settimeofday だけ見ればよいか）

時刻同期の補正には **step** と **slew** の2種類があり、調査では **step（`settimeofday`）だけを見れば十分**。
理由は「`time.Now()` の**値**を変えるのは両方だが、**値を後退（非単調に）させうるのは step だけ**」だから。

| 操作 | ログ上 | 時計への作用 | `time.Now()` の値 | 単調性 |
|---|---|---|---|---|
| **step** | `src,settimeofday` | 時刻を**不連続に書き換え**（瞬間ジャンプ） | 変わる（前後どちらにも飛ぶ） | **後退しうる** ← 順序破壊の原因 |
| **slew** | `src,adjtime`（`ntp_adjtime`） | tick の**速度を一時的に増減**して徐々に収束 | 変わる（レートが変化） | **後退しない**（遅らせるだけ・止まりもしない） |

ポイント:

- slew は「進みすぎ」を直すときも、時計を巻き戻すのではなく **“ゆっくり進ませる”** ことで補正する
  （macOS/BSD はレート変更に上限があり、概ね数百ppm）。よって **`time.Now()` が前回より小さい値を返すことは原理的にない**。
- したがって `adjust` が負の slew（＝遅らせる方向の補正）が何件あっても、**順序（作成時刻の並び）は壊れない**。
  順序を壊しうるのは**後退 step だけ**。
- Go の `time.Now()` は壁時計（CLOCK_REALTIME）に加えモノトニッククロック（CLOCK_MONOTONIC）も同時に読むが、
  後者は step/slew どちらの影響も受けない。ただし DB に保存する作成時刻や、別々の `time.Now()` 同士で
  **絶対時刻を比較**する場面ではモノトニック成分は剥がれ、**壁時計の値**が使われる。だから今回は step の後退が効いた。

> **実測（このマシン・保持ログ約20.7時間）**: slew は56件（うち遅らせる方向＝負が30件、最大でも −38ms）あったが、
> これらは一切時計を後退させていない。実際に時計が後退したのは step（`settimeofday`）の4件のみ
> （§8 参照。実害は −2.04 秒の1件）。

---

## 6. なぜこの種のテストは脆いのか

このパターンのテストは次を暗黙の前提にしている:

```
逐次の作成順 == time.Now() の単調増加 == DB の作成時刻順
```

しかし DB 仕様は `ORDER BY 作成時刻 DESC, 識別子 DESC`。次の2つに対して脆い:

1. **壁時計の非単調性（今回）** — NTP ステップやスリープ復帰で後方ジャンプすると作成時刻が逆転する。
2. **作成時刻のタイ** — 2件が同一タイムスタンプになると DB は第2キー（識別子）で tie-break するが、
   テストはそれを再現していないので作成順と食い違う。

### 後退ステップが実害になる条件（危険窓 ≈ δ）

後退ステップ1回が実害を生むには、次の3つが**同時に**必要:

1. 時計が後退する（step が起きる）
2. **順序が意味を持つデータ**が、その後退をまたいで作られる
3. それが**タイムスタンプ順で比較**される（DB の `ORDER BY 時刻` など）

後退量 δ の step は、**直前 δ 秒ぶんの時刻を“もう一度やり直す”**ことになる。
つまり「step 直前 δ 秒に作ったデータ」と「step 直後 δ 秒に作ったデータ」のタイムスタンプが重なり/逆転する。
→ **危険窓 ≈ δ**。順序を狂わせられるのは **δ より短い間隔で作られた2件**だけ。

当たる確率は「**後退量 δ ÷ データ作成間隔**」でおおよそ決まる:

- `δ < 作成間隔` → 窓が1件ぶんに満たず **まず当たらない**
- `δ ≥ 作成間隔` → 隣接ペアを逆転させうる **危険**

このマシンの実測（作成間隔 約0.6秒）での見積もり:

| 後退 step | δ（後退量） | 危険窓 | 判定 |
|---|---|---|---|
| 01:29 | 0.053 秒 | 〜53ms | 1件ぶん未満 → まず当たらない |
| 04:40 | 0.128 秒 | 〜128ms | 同上 → ほぼ無害 |
| 09:38 | 0.120 秒 | 〜120ms | 同上 → ほぼ無害 |
| **11:54** | **2.045 秒** | **〜2秒** | **約3件ぶんをまたぐ → 逆転（HIT）** |

> つまり「後退が4回あった＝失敗チャンス4回」ではない。実害を出せたのは δ が作成間隔より十分大きい
> **−2.04秒の1件だけ**。逆に、**データ作成がミリ秒間隔のような高速ケース**では、数十msの小さな後退でも
> 逆転を起こしうる（`δ ≧ 作成間隔` になるため）。

### 本番（Cloud Spanner）では基本的に起きない

この問題が現実的に起きるのは **ローカル開発機**。本番では以下の理由でまず顕在化しない:

- **作成時刻を Spanner のコミットタイムスタンプ（`PENDING_COMMIT_TIMESTAMP` / `spanner.CommitTimestamp`）で
  打つ設計**なら、時刻は Spanner サーバ側で **TrueTime に基づきコミット順に単調増加**する形で割り当てられ、
  アプリ側のローカル壁時計に一切依存しない。後退ステップの影響を受けない。
  - 一方、**作成時刻をアプリの `time.Now()` で設定している場合**（今回のケースもこれ）は、本番でも
    「アプリ稼働ホストの壁時計」に依存する点に注意。ただし次の理由で実害になりにくい。
- **GCP のホストクロックは規律されている** — Google の時刻同期はうるう秒スミアリング等で連続化されており、
  ローカル Mac で見られたような秒オーダーの後退ステップを避ける設計。後退ステップ自体がまず起きない。
- そもそも本番は**多数リクエストが並行**してデータを作るため、「逐次作成順 == 時刻順」という前提が成立しない。
  まともな設計は**順序を厳密に壁時計へ依存させない**（識別子で tie-break、ページトークンに `(時刻, 識別子)` を持つ等）
  ことが前提になっている。

> 要するに今回のフレーキーは、「**ローカル開発機の壁時計が後退しうる**」という環境特有の事情と、
> 「テストが壁時計の単調性を暗黙に仮定している」という前提の**組み合わせ**で起きるもの。
> 本番のデータ整合性そのものの問題ではない。

### 「本物の Spanner を使えば起きない」か？ → 時刻の発番元しだい

「ローカルの Spanner エミュレータではなく本物の Spanner を使えば回避できるのでは」と考えがちだが、
**これは条件付きで、今回のケースでは当てはまらない**。鍵は **作成時刻を誰が発番しているか**。

今回のコードは作成時刻を**アプリ側の `time.Now()`**（ローカルで動くサービスプロセス）で打ち、
その値をミューテーションに載せて Spanner へ渡している。Spanner（エミュレータでも本物でも）は
**受け取った値をそのまま保存するだけ**で、時刻の出どころは Spanner ではなく**ローカルアプリの壁時計**。

| 作成時刻の発番元 | ローカルのエミュレータ | 本物の Spanner |
|---|---|---|
| **アプリの `time.Now()`**（← 今回） | ローカル壁時計依存 → 後退の影響を受ける | **同じくアプリの壁時計依存 → 本物でも受ける** |
| **Spanner コミットタイムスタンプ**（`PENDING_COMMIT_TIMESTAMP`） | エミュレータは**ローカル host clock** で発番 → **受ける** | サーバ側 **TrueTime** で発番 → **受けない（安全）** |

- **今回のケース（`time.Now()` 発番）では、本物の Spanner に替えても直らない。** 時刻はローカルアプリの
  壁時計から来ており、Spanner はその器でしかない。**エミュレータが犯人なのではなく、ローカルアプリの
  壁時計の後退が犯人**。
- 「本物の Spanner なら起きない」が成り立つのは、**コミットタイムスタンプで発番する設計**にした場合のみ
  （右下のセル）。本物の Spanner はサーバ側 TrueTime で単調な時刻を割り当てるので後退の影響を受けない。
- ただし**その設計でもエミュレータは安全ではない**。エミュレータは TrueTime を持たず、コミットタイムスタンプも
  **ローカルの host clock** で生成するため、ローカル時計が後退すれば同じ問題が起きうる。

> まとめ: 「本物の Spanner で回避できる」のは **`コミットタイムスタンプ発番` × `本物の Spanner`** の
> 組み合わせに限られる。アプリの `time.Now()` で発番している限りは、エミュレータでも本物でも
> （＝ローカル実行で壁時計が後退する限り）起きうる。

### Cloud Run（本番の実行環境）ではどうか

本番はサービスを Cloud Run 上で動かしている。結論: **今回と同種の「秒オーダーの後退ステップ」はまず起きない**。
ただし「タイムスタンプ順 == 作成順」を 100% 保証するものではない。

- **後退ステップは起きにくい** — Cloud Run の実行基盤（Google 管理ホスト）のクロックは Google の時刻同期で
  規律されており、うるう秒スミアリング等で**連続・単調**に保たれ、ローカル Mac で見た秒オーダーの後退ステップを
  避ける設計。`time.Now()` が後退するという今回の犯人そのものが本番ではまず発生しない。
  → **このフレーキーは本番では再現しないと考えてよい**。
- **ただしインスタンス間スキューは残る** — Cloud Run はオートスケールで**複数インスタンス**が並行稼働する。
  各インスタンスのクロックが個別に単調でも、**インスタンス間の時刻は完全一致しない**（Google の同期は概ね
  ms 以下に収まるが 0 ではない）。レコード A をインスタンス1、B をインスタンス2 が**ごく近接した時刻**に作ると、
  `time.Now()` の値が実時間の前後関係を反映しないことがある。これは後退ステップとは**別機構**で、ズレは
  **ms オーダー**と桁が小さく、多くの場合は第2キー（識別子）の決定的な tie-break で吸収される。
- **厳密な単調順序が要るなら** — インスタンス数や時計に依存せず保証したいなら、アプリの `time.Now()` ではなく
  **Spanner のコミットタイムスタンプ（TrueTime）** を使うのが正攻法。

---

## 7. 今回の対応方針

- **テスト側の対策は見送り**。原因が壁時計の非単調性という環境要因であり、作成時刻の降順に依存する
  他のE2Eテストも同様に揺れうるため、個別テストを直しても根本解決にならず、横展開コストに見合わないと判断。
  - **【2026-08-02 追記】環境側の対策は導入した。** テストを直す代わりに、ループ実行中だけ自動時刻同期を
    止めて**後退ステップの発生源を消す**方式を実行スクリプトに組み込んだ（§12）。
    「各イテレーションの先頭で時刻を合わせる」案は**効かない**ことを実測で確認済み（§12 冒頭）。
  - なお §8 のとおり **後退ステップの発生頻度自体は当初の想定より高い**（1日に複数回）ことが後に分かったが、
    それでも実測のテスト失敗率は 114 イテレーション中 1 回。稀なのは「ステップが起きること」ではなく
    「**危険窓に着弾すること**」であり、見送りの判断はこの数字でも維持できる。
- 同種の「順序系E2Eの隣接スワップ失敗」を見たら、まず環境要因（クロックステップ）を疑い、
  実行ログのタイムスタンプ逆行と `log show`（timed）で確認のうえ**再実行**する。

### （将来 恒久対策する場合の選択肢）

- want / got を**識別子をキーに集合比較**する。
- 両者を**同一のソートキー（作成時刻, 第2キー降順）でソートしてから**比較する
  （タイには効くが壁時計逆行には無力）。
- テスト内で作成時刻を制御・注入できるようにする。

---

## 8. 発生頻度・なぜ稀か

この事象は**極めて稀**である。ローカルでのE2Eテスト実行を 2018 年から続けてきて、
2026-05-25 が**初遭遇**だった。成立には複数の低確率条件が**同時に揃う**必要がある:

1. **NTP が slew ではなく step した** — OS は普段ズレを slew（周波数の微調整で徐々に追従）で吸収し、
   `settimeofday` による一気の後方ステップは**オフセットが閾値（秒オーダー）を超えたときだけ**起こる。
   定常的に時刻同期できているマシンでは、そもそもローカル時計が数秒ずれること自体が珍しい
   （スリープ復帰・ネットワーク再接続・オフライン後の復帰・VM/コンテナの時刻ドリフト・
   高負荷で時刻同期デーモンが遅延、等が引き金になりうる）。
2. **その後方ステップが、順序を検証している2レコードの逐次作成の“隙間”にちょうど着弾した** —
   窓は数百ミリ秒〜秒オーダーと狭い。
3. **テストが `time.Now()` 由来の作成時刻順に依存**していて、第2キーの tie-break を再現していない。
4. **セットアップが長時間**（多数レコードを十数分かけて作成）→ ステップが実行中に当たる露出時間が長い。

確率はこれらの**積**なので、「長年で初めて」はむしろ自然。
裏を返すと、長時間ランニングテストだからこそ拾えた稀少事象とも言える。

> なお、近年マシン／OSバージョン／ネットワーク環境が変わって**ステップが起きやすくなった**可能性もある。
> 再発が続くようなら `log show --predicate 'process == "timed"' --info --debug | grep adjust` で
> ステップ頻度の傾向を確認するとよい。

> **【2026-07-01 再発】** 初遭遇（05-25）から約5週間後、別の一覧取得APIの長時間E2Eループで再発。
> `timed` ログに **14:51:18.931 JST の `cmd,apply,src,settimeofday,…,adjust,-2.139144182,source,NTP`**（＝約 −2.14 秒の後退ステップ）を確認。
> §8 末尾で疑った「時刻源の不安定さ＝再発リスクの兆候」が現実化した形で、**もはや「長年で1回」の単発事象ではない**。
> 05-25（−2.045秒）と 07-01（−2.139秒）はいずれも **約2秒オーダーの後退**で規模が揃っており、このマシン固有の周期的な時刻オシレーションが疑わしい。
> ただし発症した**壊れ方は別物**だった（§10 参照）。

> **【2026-08-02 三度目】** 07-01 からさらに約4.5週間後、E2Eの114回ループの **114回目だけ**で再発。
> `timed` ログに **09:37:56 の `cmd,apply,src,settimeofday,…,adjust,-2.073060632,success,1`**（＝約 −2.07 秒の後退ステップ）を確認。
> アプリログ側で検出した逆行（09:37:58.757 → 09:37:56.687 = 2.070 秒）と完全に一致した。
> 後退量は 05-25（−2.045）/ 07-01（−2.139）/ 08-02（−2.073）と**3回とも約2秒**で揃っている。
> このときは **1回のステップが2つのテストを同時に**壊し、しかも**機序が別**だった（§10 型と §11 型）。
> さらにこの回で、後退と復元に**明確な周期性**があること・**発生頻度が当初想定よりずっと高い**ことが判明した（次項）。

### このマシンでの実測（2026-05-25）

問題判明後、保持ログ範囲で `settimeofday`（＝slew ではなくハードなステップ）の apply を全件抽出した。
ただし `timed` の統合ログはこのマシンでは**約半日でローテーション**され、本日分（00:38〜12:26、約12時間）しか残っていなかった。
その範囲で計10件（前進6・後退4）。**後退（巻き戻し）は4件**:

| 時刻 (JST) | adjust | 備考 |
|---|---|---|
| 01:29:13 | −0.053 秒 | 微小 |
| 04:40:50 | −0.128 秒 | 微小 |
| 09:38:43 | −0.120 秒 | 微小 |
| **11:54:51** | **−2.045 秒** | **← 今回テストを壊した巻き戻し** |

ポイント:

- 後退4件中3件は数十〜100ms程度の微小補正で、順序に影響する可能性はほぼない。
  **実害があったのは 11:54:51 の −2.04 秒だけ**。「複数条件の積」という稀少性が実データでも裏付けられた。
- 直後の **12:26:55 に +2.05 秒の前進ステップ**があり、−2.04秒をほぼ打ち消していた。
  つまり昼前後に**約2秒の時刻オシレーション**が起きており、通常の NTP 微補正（〜数百ms）と桁が違う。
  時刻源の不安定さ・ネットワーク経路・RTC/発振器まわりが疑わしく、**再発リスクの兆候**かもしれない。

### このマシンでの実測（2026-08-02）：−2秒 → +2秒 の復元は約32分後

保持範囲（08-01 23:42〜08-02 13:58、約14時間）の `settimeofday` を全件抽出したところ12件。
**±0.1秒級の微小補正が7件**のほかに、**±2秒級のオシレーションが4件**あった:

| 時刻 (JST) | adjust | 備考 |
|---|---|---|
| **09:37:56** | **−2.073 秒** | **← 今回テストを壊した巻き戻し** |
| 10:09:58 | +2.050 秒 | **32分02秒**後に復元 |
| **10:25:56** | **−2.124 秒** | 再び後退（テスト実行外だったため無害） |
| 10:57:56 | +2.071 秒 | **32分00秒**後に復元 |

ポイント:

- 05-25 の記録（11:54:51 の −2.045 → 12:26:55 の +2.05）も **32分04秒**後の復元だった。
  **3例すべてで「−2秒の後退 → 約32分後に +2秒の前進」**という同一パターンであり、
  05-25 の時点で「疑わしい」としていた**周期的な時刻オシレーション**が実データで裏づけられた形。
- **頻度の再評価（§8 冒頭「極めて稀」の訂正）**: 約14時間の保持範囲だけで −2秒級の後退が
  **2回**（09:37、10:25）起きている。「長年で初めて」だった 05-25 時点とは頻度が桁違いで、
  **ステップの発生自体はもはや稀ではない**。
  一方この日の114回ループで実際に壊れたのは **1イテレーションのみ**だった。
  → **稀なのは「後退ステップが起きること」ではなく「それが危険窓（§6）に着弾すること」**。
  §8 冒頭の「複数の低確率条件の積」という説明は依然正しいが、その内訳のうち条件1（ステップが起きる）は
  もはや低確率ではなく、稀少性は条件2（着弾タイミング）が担っている。

### 長期監視（統合ログは短命なので追記保存する）

傾向を継続的に追うなら、ステップ発生時にファイルへ追記しておくと確実:

```bash
# 後退（巻き戻し）のみ追記
log stream --predicate 'process == "timed" AND eventMessage CONTAINS "cmd,apply,src,settimeofday"' --info --debug \
  | grep --line-buffered 'adjust,-' >> ~/clock_backward_steps.log
```

`grep 'adjust,-'` を外せば前進ステップも記録できる。

---

## 9. 教訓・チェックリスト

- [ ] 「隣接2要素の綺麗なスワップ」失敗を見たら、ソート主キーの**タイ or 逆転**を疑う。
- [ ] **「作ったはずのレコードが一覧に出てこない／件数不足」**も同じ真因のことがある（§10）。
      一覧APIが**1ページ目の上限に `time.Now()` を使い `WHERE 作成時刻 < 上限` で絞っていないか**を確認する。
- [ ] **「最新の1件が期待と違うレコードになる」**も同じ真因のことがある（§11）。
      `ORDER BY <時刻> DESC LIMIT 1` は症状が「スワップ」に見えないので、上2つのシグネチャでは気づけない。
- [ ] **ソートキーは作成時刻とは限らない。** `time.Now() + オフセット` で導出した時刻列
      （有効期限・締切など）も壁時計の非単調性をそのまま継承する。`CreateTime DESC` だけを
      grep しても脆い箇所は見つからない（§11）。
- [ ] **同一イテレーションで複数のテストが落ちたら、まず1回のクロックステップを疑う**（§4 末尾）。
      機序が違っても真因は1つのことがある。
- [ ] サーバ側の並び順が決定的か（クエリの `ORDER BY` と組み立てロジック）をまず確認する。
      **`LIMIT 1` でもタイブレーカは要る**（同値なら返る行が不定）。
- [ ] `time.Now()` 由来のタイムスタンプに順序を依存させていないか確認する。
      **壁時計は単調増加とは限らない**（NTP ステップ・スリープ復帰）。
- [ ] 長時間ループの巨大ログは、**失敗イテレーションだけ切り出してから**時刻の逆行を探す（§4 末尾）。
      OS の統合ログは約半日で消えるため、アプリログが一次証拠になることが多い。
- [ ] **長時間ループを回すなら、実行中は自動時刻同期を止める**（§12）。後退ステップの発生源を消せる。
      「イテレーション先頭で時刻を合わせる」だけでは**効かない**（ズレは蓄積型ではないため）。
- [ ] macOS なら `log show --predicate 'process == "timed"' --info --debug` で
      `settimeofday` / `adjust,-N` を探せば、巻き戻しを後追いで確定できる。
- [ ] `date -r <epoch秒>` で問題タイムスタンプをローカル時刻に変換し、ログの調査窓を絞る。

---

## 10. 別の発症形：レコードが結果から丸ごと消える（後退ステップの第2の噛み方）

§1〜§6 は「`ORDER BY 作成時刻 DESC` の結果で**隣接2件が入れ替わる**」症状だった。
だが同じ真因（壁時計の後退ステップ）は、まったく別のシグネチャでも出る。
**「作ったはずのレコードが一覧に1件も出てこない」＝丸ごと欠落**である。

### 症状

「直前に作成・確定したレコードを、その所有者の一覧取得APIで引き当てる」ヘルパが、
`... not found for <キー>=…` のように**見つからず失敗**する。作成・確定処理自体は成功しており、
さらに**直前の別の一覧取得（別条件での引き当て）は同じレコードを見つけている**のに、後続の引き当てだけが失敗する。

### 機序：`time.Now()` を「クエリの上限」に使う設計

順序系（§4）と違い、ここで壊れるのは**保存済みタイムスタンプ同士の比較**ではなく、
**保存済みタイムスタンプ vs その場で取った `time.Now()`（クエリ上限）の比較**である。

カーソルページネーションの一覧APIによくある実装:

```go
// 1ページ目は page token が空 → 上限を「今」に既定
func (t *pageToken) SetDefaultValue() {
    if t.CreateTime.IsZero() {
        t.CreateTime = time.Now()   // ← ここ
    }
}
```
```sql
-- 上限より古いものを新しい順に返す
SELECT ... FROM <テーブル>
WHERE <絞り込み条件> AND CreateTime < @createTime   -- @createTime = 上記 time.Now()
ORDER BY CreateTime DESC
LIMIT @limit;
```

正常時は「レコード作成 → その後に一覧取得」なので `作成時刻 < time.Now()` が成り立ち、必ず含まれる。
ところが**作成後・一覧取得前に壁時計が後方へ δ 秒ステップ**すると:

```
作成時刻(レコード) ≈ T
一覧取得時の time.Now() ≈ T − δ        （δ 秒巻き戻った）
⟹  作成時刻 < 上限  が偽（作成時刻の方が「未来」）
⟹  WHERE 句から漏れて 1ページ目に出てこない ＝ 丸ごと欠落
```

「直前の別引き当ては成功、後続だけ失敗」も同じログで説明できる。
**成功した引き当ては後退ステップ前、失敗した引き当ては後退ステップ後**に走っており、
2つの一覧取得の間で `time.Now()` が δ 秒巻き戻っていた（実行ログのタイムスタンプ逆行 §4 と同じ確認法）。

### 危険窓

順序系（§6）は「δ より短い間隔で作られた**2件**」が危険窓だったが、こちらは
**「後退ステップ直前 δ 秒以内に作られたレコードを、ステップ直後 δ 秒以内に引き当てる」**が危険窓。
δ が大きい（今回 −2.14 秒）ほど、作成→引き当ての間隔が短いヘルパほど当たりやすい。
本番で起きにくい理由（コミットタイムスタンプ発番なら無関係、GCP ホストクロックは規律されている）は §6 と同じ。

### 見分け方・対応

- **症状が「スワップ」ではなく「1件も見つからない／件数が想定より少ない」でも、真因は同じ**ことがある。
  §5 の `timed` ログ確認（`settimeofday` / `adjust,-N`）で後退ステップを裏取りし、環境要因なら再実行。
- 恒久対策するなら、1ページ目の上限に生の `time.Now()` を使わない（上限を持たない／十分未来に振る／
  作成時刻をコミットタイムスタンプ発番にする）等。ただし §7 同様、横展開コストの観点から見送り判断もあり得る。

---

## 11. 第3の発症形：「最新の1件」が別のレコードになる（導出された時刻列の噛み方）

§4 は「一覧の**隣接2件が入れ替わる**」、§10 は「レコードが**丸ごと欠落**する」だった。
3つ目は **「最新の1件を返すクエリが、期待とは別のレコードを1件返す」** である。
2026-08-02 のケース（§8）で、§10 型と**同時に**発生した。

### 症状

「あるユーザーの、特定ステータスの最新レコードを1件返す」内部クエリの結果を検証するテストが、
**件数は合っているのに中身が別レコード**で落ちる。

```
got = [レコードX], want = [レコードY]
```

スワップでも欠落でもないので、§9 の上2つのシグネチャでは気づけない。

### 機序：ソートキーが「作成時刻」ではなく「壁時計から導出した未来の時刻」

問題のクエリはこの形だった（一般化）:

```sql
SELECT ... FROM <テーブル>
WHERE <所有者> = @id AND <ステータス> = @status AND <期限> >= @baseTime
ORDER BY <期限> DESC     -- ← タイブレーカなし
LIMIT 1;
```

そして `<期限>` はレコード生成時にこう決まっていた:

```go
// 期限 = 「処理時点の壁時計」＋ 固定の有効期間
ExpireTime: baseTime.Add(time.Duration(expireDurationSeconds) * time.Second)
```

**有効期間が両レコードで同一**（このケースでは同じマスタ定義を共有し 6000 秒）だったため、
`期限` の大小関係は **リクエスト処理時刻の前後関係とまったく同じ**になる。よって:

```
実行順:      レコードX を生成 → （後退ステップ −δ 秒） → レコードY を生成
記録された期限: X = T + 6000              Y = (T − δ) + 6000
⟹ 期限(X) > 期限(Y)
⟹ ORDER BY 期限 DESC LIMIT 1 が「後から作った Y」ではなく X を返す
```

実測では X の処理時刻が 09:37:58.305、Y が 09:37:56.738 と記録されており、**逐次実行しているのに
記録時刻だけが1.567秒逆転**していた（§4 と同じ確認法で裏取りできる）。

### この形が見つけにくい理由

- **`CreateTime` を grep しても出てこない。** ソートキーは有効期限・締切・公開予定時刻など、
  「`time.Now()` に何かを足した列」であることがある。**壁時計から導出された時刻列はすべて
  非単調性を継承する**（単調増加変換なので順序がそのまま壊れる）。
- **`LIMIT 1` は「並び順の問題」に見えない。** 実際には「トップ1の選択」が並び順に依存している。
- **タイブレーカが無いことが二重のリスク。** 後退ステップが無くても、
  2レコードの期限が**同値**になれば返る行は不定になる（§6 の「タイ」と同じ）。
  `ORDER BY <期限> DESC, <識別子> DESC` のように決定的にしておくべき箇所。

### 危険窓

§6 と同じく **危険窓 ≈ δ**。「後退ステップをまたいで、δ 秒以内に2件が生成され、
その2件が同じソートキー計算式（同じオフセット）を共有している」場合に逆転する。
オフセットが異なる（有効期間が違う）2件なら、差がオフセット差で吸収されて逆転しないこともある。

### 見分け方・対応

- 「件数は合うが中身が違う」「最新のはずのものが取れない」を見たら、
  **そのクエリのソートキーがどう作られているか**（`time.Now()` 由来か）を確認する。
- 環境要因（後退ステップ）だと確定したら §7 同様に**再実行**でよい。
- 恒久対策するなら **タイブレーカの追加**（`..., <識別子> DESC`）が第一。
  これは後退ステップには無力だが、**タイによる不定性は消せる**うえコストが小さい。
  壁時計の非単調性まで断つなら、順序判定をコミットタイムスタンプ発番の列に寄せる（§6 参照）。

---

## 12. 対策の選択肢と有効性（2026-08-02 検討・環境側の恒久対策を導入）

§7 は「**テスト側**の対策は見送り」という判断だった。3度目の再発（§8）を受けて、**環境側**で
後退ステップ自体を起こさない方法を検討し、実行スクリプトに組み込んだ。

### 却下した案: 「ループの各イテレーション先頭で時刻を合わせる」

まず検討したのは「ドリフトが溜まって大きな補正になるのだから、各イテレーションの先頭で
合わせておけば実行中の後退ステップは減るのでは」という案。**これは効かない。**

**却下の根拠1: ズレは蓄積していない。** `innv`（NTPが返した時刻 − timed の予測時刻）は、
時間経過に比例して増えるのではなく**一定振幅（約2.07秒）で符号だけが反転**していた（§8 の表）。
09:21:58 の −2.091 → 09:37:58 の −2.073 と 16 分間ほぼ**平坦**で、ドリフトの形をしていない。
もし +2 秒がドリフトなら直近補正（07:52）から 1 時間 45 分で 317 ppm という異常値になり、実測とも合わない。

**却下の根拠2: 同じ実験が既にログの中で行われていて、失敗している。**
**09:37:56 に timed 自身が `settimeofday` で時刻を合わせ直している**。これは本案の
「先頭で合わせる」と同じ操作である。ところが**その16分後（09:53:54）には符号が反転した
+2.056 秒の差が復活**していた。合わせ直しても次のポーリング（約16分間隔）で差が戻る。
E2E の1イテレーションは約26分なので、**イテレーション中に必ず1〜2回ポーリングが入る**。

**補足: 時計は2つの状態を往復している。** `timed` の生フィールドを分解すると、
`predTime − rtc`（システム時計が単調クロックに対して持つオフセット）が2値を往復していた。

| 時刻 | innv | Δ(utc−rtc) | predTime−rtc |
|---|---|---|---|
| 09:21:58 | −2.091 | −0.053 | …136.594 |
| 09:37:58 | −2.073 | 0.027 | …136.603 |
| 09:53:54 | **+2.056** | **2.066** | **…134.540** |
| 10:09:56 | +2.050 | 0.003 | …134.549 |
| 10:25:58 | **−2.124** | **−2.114** | **…136.609** |
| 10:41:54 | +2.083 | 2.093 | …134.495 |
| 10:57:54 | +2.071 | −0.003 | …134.504 |

2値は **…134.5 と …136.6（差 約2.07秒）** で、`innv` は**一度も 0 付近に戻らない**。
「合わせても保たない」ことがこの構造からも読める。

**時刻源側は正常だった。** 設定は `time.asia.apple.com` / Network Time: On で、
DNS が返す各サーバへ直接問い合わせた実測（時計は変更しない `sntp` の読み取りのみ）は
**+0.019〜+0.041 秒で4台とも一致**していた。サーバ間のばらつきではなく、**間欠的な現象**。
2値の由来は未特定（`Setting system time … from TMTimeSynthesizer` と出るため、timed 内部の
推定器と NTP が 2.07 秒食い違ったまま固定されている線が疑わしい）。安価に試せるのは
`timed` の再起動で内部モデルをリセットすること。

### 採用した案: ループ実行中だけ自動時刻同期を止める

後退ステップの**発生源そのもの**を実行中に止める。同期を止めれば時計は自走ドリフトだけになり、
これは単調（進みが速い/遅いだけ）なので §4・§10・§11 のいずれの機序も発火しない。

**安全性の根拠（自走ドリフトの実測）**: 2値の往復を挟まない区間の `Δ(utc−rtc)` から算出すると、
このマシンの自走ドリフトは **2〜28 ppm**（960s で 0.027s ＝ 28ppm、8404s で 0.019s ＝ 2.3ppm 等）。
10 ppm 見込みなら **50時間のループでも累積は約2秒**、最悪の 28 ppm でも約5秒。しかも単調増加。
BigQuery / Firebase トークン等の外部依存に影響する水準ではない。

`~/bin/kauche_bin/e2eallruns.bash`（全サービスE2Eの1イテレーション）の先頭に組み込んだ:

```bash
# 1. 自動同期を止める（timed と競合させないため、合わせる前に止める）
sudo -n /usr/sbin/systemsetup -setusingnetworktime off
# 2. 安全な境界（テスト実行前）で1回だけ意図的に合わせる
#    macOS の sntp は -S=50ms超なら clock_settime で step / -s=50ms未満なら adjtime で slew
sudo -n /usr/bin/sntp -sS -t 5 time.asia.apple.com
# 3. … E2E 実行 …
# 4. 終了時に必ず復元（trap restore_network_time EXIT INT TERM）
sudo -n /usr/sbin/systemsetup -setusingnetworktime on
```

実装上のポイント:

- **`sudo` は必ず `-n`（非対話）で呼ぶ。** 数十時間の無人ループがパスワード入力待ちで
  止まらないようにするため。NOPASSWD 未登録なら WARNING を出して**対策なしで続行**する
  （時刻同期の衛生管理でE2E本体を落とさない）。
- **復元は `trap ... EXIT INT TERM`。** スクリプトには各サービス失敗時の `exit 1` が多数あるため、
  正常終了・異常終了・Ctrl-C のいずれでも自動同期が戻るようにする。
- **能力チェックに `sudo -n true` を使ってはいけない。** `/usr/bin/true` にも NOPASSWD 登録が
  必要になり、下記の厳密一致 sudoers では登録しても永久に警告パスへ落ちる。
  **実際に使うコマンドの終了コードで判定する。**
- sudoers は**ワイルドカードを使わず argv 完全一致**で3行だけ許可する
  （`/usr/bin/sntp *` のようなワイルドカードは任意のNTPサーバへ向けて時計を書き換えられてしまう）:

```
<user> ALL=(root) NOPASSWD: /usr/sbin/systemsetup -setusingnetworktime on
<user> ALL=(root) NOPASSWD: /usr/sbin/systemsetup -setusingnetworktime off
<user> ALL=(root) NOPASSWD: /usr/bin/sntp -sS -t 5 time.asia.apple.com
```

- 完全一致のため、**スクリプト側の NTP サーバを変えたら sudoers も同じ値に更新**する必要がある。
- `sudo -v` でタイムスタンプをキャッシュする運用は既定5分で失効するため、長時間ループには使えない。

### 導入後の実証（2026-08-02 夕方・1イテレーション）

導入直後に1イテレーション（27分）走らせて機序を確認した。**設計どおり動作している。**

| 区間 | 状態 | `timed` の `cmd,apply` | NTP ポーリング（`innv`） |
|---|---|---|---|
| 15:48:35 〜 16:15:35（テスト実行中） | off | **0 件** | **0 件** |
| 16:15:35（`on` に復元） | on | **−0.087 秒**（`on` の 1 秒以内） | 16:15:38 に再開 |
| 16:16:36 〜 16:18:19（次イテレーション） | off | 0 件 | 0 件 |
| 16:18:19（中断により `on` に復元） | on | **−0.101 秒**（`on` の 1 秒以内） | 16:18:21 に再開 |

- **off の間は `timed` が完全に静止**し、**`on` に戻すと1秒以内にステップが発生**。これが2回とも再現した。
  統計（失敗の有無）ではなく**機序**として示せているので、27分という短時間でも確認になる。
- ステップは**テスト実行外の安全な境界に閉じ込められた**。これが本対策の目的そのもの。

**検証の決め手は `systemsetup` プロセスのログとの突き合わせ**だった。`log show --predicate 'process == "systemsetup"'`
で off/on を実行した時刻を取り、`settimeofday` の時刻と並べると、ステップが実行中に起きたのか
境界で起きたのかが確定する（今回は両方とも 30〜100ms 以内で一致した）。

> **誤診の罠（実際に一度誤診した）**: ループのカウンタファイルに記録される時刻は
> **各イテレーションの「開始」時刻**である。これを「前のイテレーションの終了時刻」と読むと、
> 境界で起きたステップを「実行中に起きた」と誤判定し、「対策が効いていない」という逆の結論に至る。
> **イテレーションの終了時刻は、カウンタではなく `on` を実行した時刻（= `systemsetup` のログ）で確定する。**

残った観察:

- **時計が約2秒進んだ状態は解消していない。** `on` 直後のポーリングは `innv` = −1.996 / −1.970 で、
  §8 で見た2値の往復が続いている。ただし `timed` が適用したのは 0.1 秒だけで、残りは持ち越された
  （対策前に −2.07 秒を全量適用していたのとは挙動が違うが、これは `timed` 側の補正量の方針であり
  このスイッチとは無関係と見られる）。いずれにせよ**補正は `on` の直後にしか起きない**のでテストには影響しない。
- **自走ドリフトの実測はできなかった。** `innv` が −2 秒のまま残り適用も clamp されるため、
  ドリフト分を分離できない（27分で2秒進むのは 1234 ppm 相当で、クリスタルのドリフトでは説明できない量。
  「2値の往復は間欠的な現象」という §12 冒頭の見立てと整合する）。
- テスト失敗は0件だったが、**27分では検出力が無い**（上の表で言えば期待ステップ数 0.4 件相当）。
  失敗の有無で判定したいなら §12 の検出力の表どおり約12時間必要。

### 残る限界

- 自動同期を止めても**スリープ復帰による時計のジャンプ**は防げない（今回のケースでは
  `pmset` の DarkWake 時刻とステップ時刻が一致せず、無関係と確認できている）。
- 「先頭で合わせる」操作自体は、**この案と組み合わせる場合に限り**意味を持つ
  （意図的なステップをテスト実行外の安全な境界に閉じ込める役割）。単独では効かない。

---

## 参考

- 関連: [現在時刻から導出される値のハードコードによる Flaky Test](./hardcoded-season-from-current-time.md)
- 関連: [時刻境界で失敗するフレーキーテストの原因と対策](./flaky-test-time-boundary-patterns.md)
- 関連: [逐次ループの並行化におけるループキャリー依存性の見落とし](./loop-carried-dependency-in-concurrent-test-setup.md)
