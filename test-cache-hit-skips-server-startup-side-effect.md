# テストランナーの結果キャッシュがヒットして「テストの副作用」が実行されない

## 概要

E2E ハーネスが「被テストサーバを起動する手段」としてテストランナー（`go test`）を流用している構成で、
そのランナーの**結果キャッシュがヒット**すると、プロセスは前回の出力を再生して即 exit 0 する。
サーバは一度も起動しないため、テスト本体は 1 件も落ちていないのに E2E 全体が
「サーバが ready にならなかった」で失敗する。

この型の厄介さは 3 点。

1. **失敗がテストコードと無関係**: `FAIL:` 行がゼロなので、テストを読んでも原因が見つからない。
2. **成功したように見えるログが出る**: 前回の出力が再生されるので、`PASS` も所要時間も揃っている。
3. **頻度が極端に低い**: 「毎回変わる値」がキャッシュ判定に入っているため通常はミスする。
   その値が偶然一致した回だけ発火するので、数十周に 1 回しか出ない。

一般化すると **「結果の再生」を前提とするキャッシュ機構を、「副作用の実行」に使ってしまった**
という設計の型であり、キャッシュを持つあらゆるビルド/テストツール（go test, Bazel, nx, turbo,
pytest-cache 系プラグイン等）に同じ構造がある。

## 症状

- テストランナーの出力に `FAIL:`（個別テストの失敗）が 1 行も無いのに、パッケージ単位で `FAIL` / 非 0 終了。
- ハーネスが「サーバが起動しない」系のエラーを出す（例: `the service terminated before becoming ready`）。
- 失敗した試行の所要時間が異常に短い（起動待ちタイムアウト分だけ）。
- 出力に**内部矛盾**がある。これが決定的な手掛かりになる:

```
(23:35:26 に開始した試行の出力)
=== RUN   TestVoid
2026/07/27 23:13:37 Terminate request is received   ← 22 分前のタイムスタンプ
--- PASS: TestVoid (301.67s)                        ← 27 秒しか経っていないのに 301 秒
ok  <pkg>  (cached)                                 ← 実行されていない印
...
InvokeService failed: the service terminated before becoming ready
FAIL <pkg>/e2etests  28.538s
```

- ループ実行の監視スクリプトが「テスト失敗」として扱うと、失敗テスト名が取れずアラートが情報ゼロになる。

## 前提となる構成

サーバをカバレッジ計測付きで起動したい E2E では、次のような「テストランナーをサーバ起動に流用する」
ハーネスがよく使われる。

```
テストスイート側プロセス
  ├─ fake 外部サービスを起動（動的ポート）
  ├─ 起動用の子プロセスを spawn: make target → go test -coverprofile=... .
  │     子プロセス側のテスト関数は「main() を goroutine で起動 → ready 通知 → 終了要求を待つ」だけ
  ├─ ready 通知（TCP 接続など）を待つ
  ├─ テスト本体を実行
  └─ 終了要求を送ってカバレッジを書かせる
```

サーバ起動は「テスト関数の副作用」であって「テストの結果」ではない。
ところがランナーから見れば、これは単に「PASS したテスト」なので、**次回は結果をキャッシュから返してよい**
と判断される。ここが構造的な欠陥。

## 根本原因

### 1. キャッシュ判定の入力モデルを把握していなかった

Go の場合、テストキャッシュは 2 段のルックアップ（`cmd/go/internal/test/test.go`）。

- **1 段目 `testID`**: テストバイナリの内容 + キャッシュ可能なフラグ列。
  ここで得られるのが「前回そのバイナリが読んだ環境変数とファイルのリスト」（testlog）。
- **2 段目 `testInputsID`**: その testlog に載っている名前について、**今の値**を再ハッシュする
  （`computeTestInputsID` / `hashGetenv` = `os.LookupEnv`）。
  ファイルはモジュール/GOPATH/GOROOT の root 外なら**照合対象から外される**
  （`search.InDir(name, a.Package.Root) == ""` なら `break`）。

つまり「前回と今回で、テストが読んだ環境変数の値が全部一致する」なら**ヒットする**。
起動用ターゲットに `-count=1` が無ければ、毎回この判定にかけられている。

### 2. 「毎回変わるから安全」が、実は準決定的だった

このハーネスでは fake 外部サービスのアドレスを環境変数で子プロセスへ渡している
（`FAKE_X_BASE_URL=http://127.0.0.1:<動的ポート>` 等）。
動的ポートは毎回変わる想定なので、通常はキャッシュがミスし、結果としてサーバは正しく起動していた。
**キャッシュミスに偶然依存して動いていた**わけである。

ところが OS のエフェメラルポートは「ランダム」ではない。macOS では**プロセス横断の単一の
単調増加カウンタ**（49152–65535 の 16384 個）から配られる。最小プログラムを 3 回連続実行すると:

```
57883 57884 57885 57886 57887
57888 57889 57890 57891 57892
57893 57894 57895 57896 57897
```

E2E の 1 周がちょうど 16384 個強のポートを消費すると、カウンタは 1 周ラップして
**ほぼ同じ位置に戻ってくる**。実際、16 周のループから抽出した fake サーバのポートは
毎周わずかに下降するだけだった:

```
64042 → 63628 → 63531 → 62844 → 62128 → 61637 → 61096 → 60794
      → 60202 → 59753 → 59187 → 58704 → 58444 → 57739 → 57430
ずれ幅: 414, 97, 687, 716, 491, 541, 302, 592, 449, 566, 483, 260, 705, 309
```

fake サーバは起動時に連続して bind されるので、**カウンタ位置がひとつ一致すれば全部一致する**。
上のずれ幅の分布から P(ずれ = 0) は数百分の 1 程度。だから「数十周に 1 回」で発火し、
短いテストや CI の単発実行ではまず出ない。

### 3. 失敗を「テスト失敗」として集計していた

ループ監視スクリプトが `FAIL:` 行を数えていたため、この失敗は「失敗テスト 0 件なのに異常終了」となり、
通知に情報が乗らなかった。ハーネス層の失敗を別クラスとして扱う設計になっていなかった。

## 調査方法（一般化した手順）

確率的にしか起きない事象は「再現を待つ」のではなく、**メカニズムを段に分解して各段を個別に確証する**。

1. **`FAIL:` 行がゼロなら、まずハーネス／ツールチェーン層を疑う。**
   テストコードを読み始める前に「そもそもテストが実行されたのか」を確認する。
2. **出力の内部矛盾を探す。** タイムスタンプが過去、所要時間が試行時間より長い、
   まだ起きていない事象のログがある——これらは「出力が再生された」一次証拠。
   ツール固有の印（`(cached)`, `FROM CACHE`, `cache hit` 等）も同時に確認する。
3. **キャッシュ実装のソースを読み、照合入力のモデルを確定する。** 推測で終わらせない。
   「何がキャッシュキーに入り、何が入らないか」が分かると、次の実験が設計できる。
4. **キャッシュに残っている入力リストの実体を読む。** Go なら `$(go env GOCACHE)` 配下で
   `# test log` で始まるファイルを探し、記録された getenv / open を実測する。

```bash
find "$(go env GOCACHE)" -type f -newermt "<窓の開始>" ! -newermt "<窓の終了>" -size -2000k \
  -print0 | xargs -0 grep -l "^# test log"
# → getenv N 件 / open は root 外のみ（= 照合は実質環境変数だけ）と確認できる
```

5. **差分実験でメカニズムを確証する。** 1 変数だけ変えて入力 ID が変わるかを見る。

```bash
GODEBUG=gocachetest=1 <FOO=A> go test <同じフラグ> .   # → input ID ae4d9295...
GODEBUG=gocachetest=1 <FOO=B> go test <同じフラグ> .   # → input ID dc3b6efc...
```

6. **「毎回変わる」と信じていた値が本当に毎回変わるか、履歴から時系列で確かめる。**
   ここが今回の分岐点だった。ログ全体（2.4GB）からポートを抽出し、周回ごとに集計すると
   ランダムではなく単調ドリフトだと分かる。

```bash
grep -aboE '(localhost|127\.0\.0\.1):[0-9]{5}' result.txt \
  | awk -F: '$3+0>=49152 {print $1, $3}'   # バイトオフセット付きで抽出し周回ごとに束ねる
```

7. **OS/ランタイムの振る舞いは最小プログラムで実測する。** 「ランダムだと思っていた」割り当てが
   単調カウンタだった、という事実はドキュメントを読むより 10 行のプログラムで早く確定する。
8. **調査自体の副作用を管理する。** 調査で走らせた失敗 run がキャッシュを汚さないこと
   （Go は FAIL した run の入力リストを保存しない）を確認してから、本番のループ環境で実験する。

## 解決策

### 一次対策: 副作用目的の実行はキャッシュ判定に載せない

```diff
-	go test -v -tags=e2e -race -timeout=600m -coverprofile=e2e_coverage.out -coverpkg=... .
+	go test -count=1 -v -tags=e2e -race -timeout=600m -coverprofile=e2e_coverage.out -coverpkg=... .
```

`-count=1` はキャッシュを無効化する慣用句（他ツールなら `--no-cache` 等）。
テスト実行時間は変わらない（そもそも毎回実行されるべきものなので）。

### 二次対策: ハーネス側で「ready 通知なしの正常終了」をエラーにする

キャッシュヒットの子プロセスは **exit 0** で終わる。ここを「起動成功」と誤認すると、
後続の全テストが謎の接続エラーで倒れ、原因が数段遠のく。

```go
select {
case <-ready:
    // 正常起動

case err := <-exitedErr:
    // exit 0 の premature 終了（例: キャッシュヒットで実行がスキップされた）でも err は nil。
    // nil を返すと呼び出し側が起動成功と誤認するため、必ずエラーにする。
    return nil, wrap(join(errors.New("the service terminated before becoming ready"), err))
}
```

今回はこの防御が既に入っていたおかげで、失敗が起動フェーズに局在し 1 行で原因に辿り着けた。

### 三次対策: 同型の設定をリポジトリ全体で揃える

1 箇所だけ直すと、別サービス・別ハーネスで同じ事象が再発する。
同じ役割のターゲットを機械的に洗い出して一括で揃え、`make -n` 等で展開結果を確認する。

```bash
grep -rn "e2ecoverage-internal" -A4 --include=Makefile . | grep "go test"
# → -count=1 の有無を一覧化して全サービス揃える
```

（今回は 10 サービス中 4 つには既に `-count=1` があった = 過去に誰かが踏んで局所的に直した痕跡。
**「一部にだけ入っている謎のフラグ」は過去の障害の化石**であり、理由を掘って全体に広げるべきサイン。）

### 設計上の教訓としての対策

「テストランナーで副作用を起こす」構成をやめるのが本筋の解ではある
（専用バイナリを `go run` / ビルド済みバイナリで起動する等）。
ただしカバレッジ計測をテストランナーに依存している場合は移行コストが高いので、
現実解は「キャッシュ無効化 + ハーネス側の premature-exit 検知」の二段構え。

## 一般化・チェックリスト

- [ ] テストランナー（またはビルドツール）の**キャッシュ判定に何が入るか**を把握しているか。
      環境変数・ファイル・フラグのうち、どれが照合され、どれが無視されるか。
- [ ] **副作用のためにテストを実行している経路**に、キャッシュ無効化フラグが付いているか
      （サーバ起動、DB セットアップ、コード生成、外部システムへの投入 等）。
- [ ] 「毎回変わる値に依存して結果的にキャッシュがミスしている」構成になっていないか。
      その値の生成器は本当にランダムか、**単調カウンタ・ラップする有限空間**ではないか
      （エフェメラルポート、PID、テンポラリ名、連番 ID）。
- [ ] 子プロセスが **ready 通知なしに exit 0** した場合を、エラーとして扱っているか。
- [ ] ループ監視・CI の集計が、「テスト失敗 0 件だが異常終了」を**別クラスの失敗**として
      識別・通知できるか（テスト名が取れないアラートは調査を止める）。
- [ ] 同型の設定がリポジトリ内で揃っているか。一部にだけ入っている不揃いなフラグの由来を調べたか。

## 教訓

- **`FAIL:` が 1 行も無い失敗は、テストコードではなくハーネス／ツールチェーンの層を見る。**
  「テストが実行されたか」を最初に確認するだけで、探索範囲が桁で変わる。
- **出力の内部矛盾（時刻・所要時間・順序）は「再生された出力」の一次証拠。**
  「なぜ 27 秒の試行に 301 秒の PASS があるのか」を素通りしないこと。
- **「ランダムだから衝突しない」は仮説であって前提ではない。** OS のリソース割り当ては
  準決定的なことがあり、周期的なワークロードでは値が周期的に一致する。
- **稀な事象を確率で片付けず、周期性を測る。** ずれ幅の分布まで測れば
  「数百周に 1 回」「今後も再発する」という再発見積もりまで出せる。
- **妥当そうな修正で症状が消えても、真因を証明したことにはならない**
  （→ [単一コネクションの転送スタック](./single-connection-stall-hangs-fixed-deadline-write.md) の教訓と同型）。
  今回はメカニズムを 3 段（キャッシュ判定モデル → 入力の実体 → 値の周期性）に分解し、
  各段を実測・差分実験で確証してから修正した。
- **キャッシュは「結果」を再生する仕組みであって「副作用」を再生しない。**
  副作用を目的にした実行にキャッシュを噛ませてはいけない。

## 関連

- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) — 数十周に 1 回の事象は
  長時間ループでしか表面化しない。逆に言えばループを回している限り必ず当たる。
- [エミュレータへの書き込みが単一コネクションの転送スタックで稀にタイムアウトする](./single-connection-stall-hangs-fixed-deadline-write.md)
  — 「妥当そうな仮説で症状が消えても真因ではない」型。調査態度の教訓が共通。
- [プロセス共有キャッシュによる test-scoped fake override の無効化](./process-shared-cache-vs-tid-scoped-fake.md)
  — こちらは「サービス内のキャッシュ」がテストの意図を貫通する型。本件は「ツールチェーンのキャッシュ」が
  テストの実行自体を消す型で、層が違うだけで構造は同じ。
