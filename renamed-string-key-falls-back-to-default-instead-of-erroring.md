# 文字列キーで疎結合された設定の改名が、参照側に届かず既定値へ静かに落ちる

## 概要

feature flag・環境変数・DI コンテナのキー・実験設定・メトリクス名・権限スコープのように、
**文字列のキーで疎結合された設定値**がある。定義側でキー名を改名したとき、そのキーを
参照している箇所（とくに**テストが値を注入するための専用経路**）に改名が届いていないと、
参照は一致しない。

問題は、多くの設定解決層が**不一致をエラーにせず、既定値へフォールバックする**ことにある。
既定値は安全側（無効・OFF・0・空）に倒してあるのが普通なので、システムは
「設定が与えられなかったときに正しく振る舞っている」ように見える。
**設定ミスと正常動作が、外から区別できない。**

結果、テストは**機能が有効な場合の期待だけを外す**。無効な場合を期待するケースは通り続ける。
一部だけが落ちるため、「機能そのものは動いているが、特定条件のデータだけ壊れている」という
誤った見立てへ誘導される。実際には、その機能は**一度も有効になっていない**。

核心は改名それ自体ではなく、**不一致が観測不能であること**にある。
型検査は文字列キーの一致を見ない。設定解決層は「知らないキー」と「値が無いキー」を区別しない。
既定値は安全側なので異常に見えない。三重に検出器が外れている。

## 症状

- 同一スイートの連続実行で、ある周から特定テストの**一部のサブケースだけ**が落ち始める。
- 落ちるのは「機能が**有効**なとき」を期待するケースのみ。
  「無効なとき既定値」を期待するケースはすべて PASS する。
- 期待値と実測値の関係は補数でも逆数でもない。実測値が**一律に既定値**（0・false・空・null）になる。
- 設定の不一致を報告するログ・警告・エラーが**一切出ない**。
  異常系のログを探しに行っても何も見つからず、入力データ側の不整合を疑い始めてしまう。
- 同一バイナリなら毎回落ちる決定的な失敗だが、長時間ループでは「N 周目から突然」に見える。
- 競合・タイムアウト・順序依存の痕跡がログに無い。

```
    xxx_test.go:NN: FieldV = 0, want 5000000      ← 機能が有効な前提の期待  … FAIL
    xxx_test.go:NN: FieldV = 0, want 10000000     ← 機能が有効な前提の期待  … FAIL

    （同一テスト内の他のサブケース ＝「既定値 0 を期待」は すべて PASS）
      - 設定値が 0 のとき 0                        … PASS
      - 設定が無いとき 0                           … PASS
      - 機能を無効にしたとき 0                     … PASS   ← 注入が壊れていても通る
```

同じスイート内で「同じ設定を注入する別のテスト」も同時に落ちる。
1 本の共有ヘルパー経由で注入していれば、症状は必ず**ヘルパー単位で揃う**。

## 前提となる構成

- 設定値が**文字列キー**で参照されている（`GetBool(ctx, "key_name")` のような形）。
  キーは定義側と参照側で別々に書かれており、コンパイラは両者の一致を検証しない。
- 設定解決層に**多段のフォールバック**がある。典型は次の順:
  1. リクエスト単位の override（テストからの注入）
  2. 取得済みの設定ソース（設定サービス・設定ファイル）
  3. 環境変数
  4. コンパイル時の既定値
  各段は「見つからなければ次へ」で、**どの段でも見つからなかった事実を報告しない**。
- テストが値を注入する**専用の裏口**がある（デバッグ用ヘッダ・テスト専用フック等）。
  この経路は本番の定義（多くは非公開の定数）に触れられないため、
  **テスト側がキー文字列を手で複製している**。
- 既定値が安全側（無効）に固定されている。「取れなければ止める」という設計判断そのものは正しい。
- 定義側の改名と、参照側の追随が**別の変更**として並行している。
  担当者が別である必要はない。**同一人物が同時に抱えている複数の変更**でも同じことが起きる。
- CI が変更単位（プルリクエスト等）でのみ走り、**base が進んでも自動では再実行されない**。
  ブランチ保護で「base が最新であること」を要求しておらず、マージキューも無い。
- 変更が**長く滞留**することがある（レビュー待ち・依存待ちで数日〜数週間）。
  滞留期間はそのまま「CI 結果の古さ」になる。

## 根本原因

### 1. 文字列キーは型検査の外にあり、改名は静かな契約変更になる

キーの改名は、シグネチャも型も値域も変えない。IDE の rename も、片方が文字列リテラルなら届かない。
にもかかわらず、**参照側から見れば「この設定を指す名前」という契約が変わっている**。

```
# 変更前（定義側）
KEY = "OLD_STYLE_KEY_NAME"

# 変更後（定義側）— 参照側は旧名のまま
KEY = "new_style_key"
```

参照側が旧名で注入し続けても、コンパイルは通り、実行時例外も出ない。
**唯一の検出器は「その設定が効いていることを確かめるテスト」だけ**になる。

### 2. 設定解決層は「知らないキー」と「値の無いキー」を区別しない

注入された override は、通常キーで引かれる連想配列に積まれる。
評価時に引くのは**評価対象のキー**なので、別名で積まれた値は**参照されないまま捨てられる**。

```
注入:   overrides["OLD_STYLE_KEY_NAME"] = true
評価:   overrides["new_style_key"]      → 見つからない → 次段へ
        設定ソース                       → 未取得／未定義 → 次段へ
        環境変数                         → 未定義 → 次段へ
        コンパイル時の既定値             → false
```

どの段にも「渡されたが使われなかった override がある」ことを気づく機構が無い。
**捨てられた注入は、注入されなかったことと同じ**として扱われる。

### 3. 既定値が安全側だと、設定ミスが正常動作に化ける

「取得できなければ無効にする」は障害設計として正しい。しかしその正しさが、
**設定ミスをエラーではなく“意図した無効化”に見せる**。

- 監視から見て異常ではない（エラー率も遅延も変わらない）
- ログから見て異常ではない（そもそも何も出ない）
- 挙動から見て異常ではない（「機能を止めているときの正しい挙動」と同一）

安全側フォールバックは**障害には効くが、設定ミスには効かない**。
むしろ設定ミスを覆い隠す方向に働く。この非対称を意識しておく必要がある。

### 4. テスト専用の注入経路が、キー文字列の複製を強いる

本番のキー定数はモジュール内に閉じている（非公開）ことが多い。
一方テストは別モジュールから注入するため、**同じ文字列を書き写す**しかない。
書き写した瞬間に、単一の情報源（single source of truth）が壊れる。

これは「テストが本番の内部に踏み込まないように」という良い設計判断の副作用として生まれる。
善意の分離が、検証不能な複製を作っている。

### 5. 改名が参照側の移行より先に入ると、無効区間ができる

段階移行の途中では、次のような順序が起きやすい。

```
段階 1: 新方式の基盤を導入する（旧方式と併存。実際の参照はまだ旧方式のまま）
        └ このとき「新方式の名前」へキーを改名しておく
段階 2: 参照側を新方式へ切り替える            ← 後続作業（別チケット）
```

段階 1 と 2 の間、参照側は**旧方式の解決層で新方式の名前を引く**。
新しい名前は旧方式の設定ソース（環境変数など）に存在せず、
古い名前はもうコードに無い。結果、**旧方式でも新方式でも値を与えられない区間**が生まれる。

区間の長さは「後続作業がいつ着手されるか」次第で、数日〜数週間になりうる。
その間、その設定は**制御不能**である。

### 6. 疎通確認ログが別経路を見ていると、ログと実挙動が食い違う

段階移行では「新方式が繋がっているか」を起動時ログで確かめることがある。
そのログが**新方式の解決層**を見ている一方、実際の処理は**旧方式の解決層**を通っていると、

```
起動ログ:  feature X enabled = true    ← 新方式の設定ソースを読んでいる
実際の応答: 常に既定値（無効）          ← 旧方式の解決層を通っている
```

という乖離が起きる。ログを信じると「有効になっているのに結果が出ない」という
最も迷う形の調査に入り込む。**疎通確認は、実際の呼び出しが通る経路で行わないと意味がない。**

## なぜ CI で止まらずマージされるのか

ここまでの機構は「壊れた統合状態がどう見えるか」の話である。
では**なぜその状態が CI をすり抜けて統合ブランチに入るのか**。
消費側のテストは存在しており、改名後にそれを実行すれば必ず落ちる。にもかかわらず止まらない。

### CI の緑は「いつの base での緑か」を持っている

変更単位の CI は、多くのホスティングで **head を base にマージした結果**で走る。
一見すると統合状態を検証しているように見える。だが走るのは
**そのイベントが発生した時点の base** であり、**base が進んでも自動では再実行されない**。

マージ可否の判定に使われるのは「最後に走ったときの base」での結果である。
滞留が長いほど、その base は実際にマージされる先から離れていく。

```
T0   変更 Y（改名）の CI 実行 … 緑     base に消費側テストはまだ存在しない
          ⋮   （数日が経過。base は大きく前進する）
T1   変更 X（消費側テスト追加）の CI 実行 … 緑   base に改名はまだ存在しない
T2   変更 X をマージ
T3   変更 Y をマージ  ← 判定の根拠は T0 の結果。再実行されていない
T4   X + Y の組み合わせが初めて実行される ＝ 統合ブランチを取り込んだ誰かのローカル
```

X の CI も Y の CI も、**相手を含まない base** で緑になっている。
実際にマージされた組み合わせは、どの CI も一度も実行していない。
「両方とも緑だった」は事実だが、「両方を同時に含む状態が緑だった」は一度も成立していない。

### 増幅する条件

- **base 最新要求が無い / マージキューが無い**: 任意に古い CI 結果のままマージできる。
  古さに上限が無いことが本質で、数分でも数日でも同じ扱いになる。
- **auto-merge（承認が揃った瞬間に自動マージ）**: 既存の CI 結果を再検証せずマージを実行する。
  「承認待ちの間に base が進む」ケースをそのまま通す。
- **長期滞留**: レビュー待ち・依存待ちで数日置かれた変更は、CI 結果も同じだけ古い。
  滞留の長い変更ほど、この型の危険度が上がる。
- **重いテストほど再実行されない**: E2E のように実行コストが高いジョブは、
  「必要になったら回す」運用になりやすく、base 更新のたびの再実行から真っ先に外れる。
- **同一人物が並行して抱える複数の変更を、短時間に連続してマージする**:
  n 本の変更が同時に在庫していれば組み合わせは n(n-1)/2 通りある。
  自分の変更同士であっても、その全通りを頭の中で追跡することはできない。
  むしろ同一人物・同一領域の変更群は、共有する識別子（設定キー・型名・テストヘルパー）が
  多いぶん**衝突確率が高い**。
  「相手の変更を確認する」という対策は、**確認相手が自分自身でも機能しない**。
  加えて、滞留していた変更をまとめて投入すると、後続の変更の CI 結果はすべて
  「先行の変更がマージされる前の base」のままになる。連続マージの間隔が
  CI の実行時間より短ければ、実質どれも再検証されない。

### 「CI が緑」の意味を正確に言い直す

> **CI が緑であることは、「過去のある時点の base と組み合わせたときに緑だった」以上を意味しない。**

この言い換えを前提にすると、レビューで確認すべきことが変わる。
「チェックが緑か」ではなく、**「その緑はいつの base で得られたか」**を見る必要がある。
そして後者は、CI の完了時刻とマージ時刻の差として**機械的に測れる**。

### 検出はできる ― 完了時刻とマージ時刻の乖離

CI の完了時刻とマージ時刻の差が大きい変更は、実際のマージ先を検証していない。
これはホスティングの API から取得できる値であり、判定に人の注意力を要しない。

- 差が数時間を超える変更を、マージ前に警告する（あるいはブロックする）。
- 事後の分析としても使える。統合ブランチが壊れたとき、
  関係する変更の「CI 完了 → マージ」の間隔を並べると、原因が粒度ではなく**鮮度**にあると分かる。

## なぜ「フレーク」に見えて、実はフレークでないか

- **見え方**: 70 周成功して 71 周目で落ちた ⇒ 蓄積劣化・並行干渉・時刻境界を疑いたくなる。
- **実際**: ループの途中で被テストバイナリが**入れ替わっていた**。
  71 周目が新しいコードでの最初の実行であり、同一バイナリなら毎回落ちる決定的な失敗。
- **紛らわしさを増す要素**:
  - 失敗が**一部のサブケースだけ**なので、「テスト全体が壊れた」ようには見えない。
  - 実測値が既定値なので、「そういう入力条件だったのでは」という説明が成立しそうに見える。
  - 上流が修正を入れると、次の更新で何もしていないのに直る。
- **集計上の害**: 「不安定なテスト」として数えられると、原因（キーの複製・改名手順）が
  指標に現れない。同じ構造が別の設定・別のテストで再発する。

## 見落としを生む非対称性 ― 通ったケースのほうが問題

この型でいちばん重要なのは、**落ちたケースより通ったケース**である。

| サブケース | 期待値 | 注入が効いている | 注入が壊れている | 判定 |
|---|---|---|---|---|
| 機能を有効にしたとき V | 非既定値 V | V | 既定値 | **FAIL**（正しく検出） |
| 機能を無効にしたとき既定値 | 既定値 | 既定値 | 既定値 | **PASS**（偽陰性） |
| 設定が無いとき既定値 | 既定値 | 既定値 | 既定値 | **PASS**（偽陰性） |

「機能を無効にしたとき既定値になること」を確かめるケースは、
**注入経路が完全に壊れていても通る**。無効化を注入しなくても既定が無効だからである。
つまりこのケースは、意図した「無効化が効くこと」を一切検証していない。

一般化すると:

> **否定側の期待値（既定値・空・エラーにならないこと）だけで構成されたテストは、
> 入力が届いていなくても通る。**

肯定側（非既定値を期待するケース）が 1 本も無いテストは、
「入力が届いていること」を誰も保証していない。
そして肯定ケースが後から別作業で追加される構成では、その保証は長期間欠けたままになる。

## 調査方法（一般化した手順）

1. **決定的かどうかを最初に切り分ける**。同じバイナリで当該テストだけを数回単体実行する。
   毎回落ちるならフレークではない。ここを飛ばすと環境要因の調査に時間が溶ける。
2. **失敗した値の分布を見る**。実測値が**すべて既定値**（0・false・空）に揃っているなら、
   計算結果が誤っているのではなく**入力が届いていない**と考える。
   補数関係・逆数関係なら「意味の反転」（別の型）、既定値一律なら「入力の不着」。
3. **通ったケースを疑う**。同一テスト内で PASS しているケースが
   「既定値を期待するもの」ばかりなら、それは偽陰性である可能性が高い。
   PASS の内訳が判断材料になるのは、この型の特徴である。
4. **別の失敗経路が出すはずのエラーログの不在を確認する**。
   値が既定値になる経路が複数あるとき（入力不着／データ不整合／内部エラー）、
   後者は必ず何らかのログを出すよう作られていることが多い。
   **そのログが無いこと**が、入力不着を指す消極的証拠になる。
5. **設定解決の経路を、キー文字列で機械的に追う**。
   注入側が使うキー文字列と、評価側が使うキー文字列を、
   リポジトリ全体の grep で突き合わせる。**この型は静的に検出可能**である
   （意味の反転と違い、文字列の不一致は目視・機械の両方で見える）。
6. **どの解決段まで到達したかを、フォールバックの終端から逆算する**。
   最終段が環境変数なら、その環境変数が**どこにも定義されていない**ことを確認する。
   定義が無いなら「そもそも値を与える手段が存在しない」と確定できる。
7. **コードが入れ替わった時点を特定する**。長時間ループでは「いつのバイナリを走らせていたか」が
   曖昧になる。**新しいコードにしか存在しないログ行**（新規追加の起動ログなど）が
   ログ全体のどこから出現するかを見ると、入れ替わりの時刻が 1 行で確定する。
   出現が 1 回だけで、それが落ち始めた周の直前なら、それがバイナリ更新の瞬間である。
8. **関係する変更の ancestry を確認する**。定義側の改名と参照側の追加が、
   互いを取り込まないまま統合されていないかを機械的に確かめる。
   どちらも相手を含まないなら、統合状態は一度も検証されていない。
9. **各変更の「CI 完了時刻」と「マージ時刻」の差を測る**。
   ancestry の確認が「何を含んでいたか」を示すのに対し、この差は
   「**その緑がどれだけ古い base で得られたか**」を示す。
   差が数時間〜数日あるなら、その CI 結果は実際のマージ先を検証していない。
   「CI は緑だったのになぜ通ったのか」という問いは、ここで完全に説明がつく。

## 解決策

### 一次対策: 参照側にキー文字列を複製させない

キーは**単一の定数**を共有する。テストから参照できないのが理由で複製しているなら、
その定数を参照可能にする（公開する・共有モジュールへ切り出す）。

- 複製が無ければ、改名は定義の変更だけで全参照に伝播し、この型は原理的に発生しない。
- 「テストのために本番の API を広げるのは避けたい」という反論は正当だが、
  **広げるのは定数 1 個**であり、代償として得るのは「不一致が起こりえない」という保証である。
- 公開したくない場合の次善策は、キーを列挙した**単一の登録簿**を持ち、
  定義側・参照側の双方がそこから引く形にすること。

### 二次対策: 未知キーの注入を失敗させる

テスト専用の注入経路に限って、**どの設定にも対応しないキーの注入をエラーにする**。

- 本番の解決経路は安全側フォールバックのままでよい（そこを厳格にすると障害耐性が下がる）。
- 厳格にするのは**テスト専用経路だけ**。ここは「意図した注入が効かなかった」ことが
  常に不具合であり、黙って捨てる理由が無い。
- 実装は「注入されたキーが既知の設定名の集合に含まれるか」を検査するだけで足りる。
- これが入っていれば、今回の型は**改名した瞬間に、最初のテスト実行で明示的に落ちる**。

### 三次対策: テストに「肯定ケース」を必ず含める

- 設定を注入するテストには、**非既定値を期待するケースを最低 1 本**含める。
  これが「注入経路が生きていること」の唯一の証明になる。
- 否定ケース（既定値を期待）だけのテストは、入力が届かなくても通ることを意識する。
- レビュー観点として明文化する: 「この設定を注入するテストに、肯定ケースはあるか」。

### 四次対策: 識別子の改名を「契約変更」として扱う

- 文字列キー・enum の文字列表現・メトリクス名・イベント名・ヘッダ名の改名は、
  型が変わらなくても**契約変更**である。改名 PR の手順に「旧名の全参照を洗う」を含める。
- 洗い出しは grep で機械的にできる。**できるのにやっていないだけ**という点で、
  意味の反転（静的検出が原理的に不可能）より状況は良い。手順化すれば消える。
- 参照側の追随を別作業に回す場合は、**改名を追随と同じ作業に含める**か、
  少なくとも「改名後・追随前」の区間で設定が制御不能になることを明示して合意する。

### 五次対策: マージ判定を「実際にマージされる組み合わせ」で行う

前四つは「この型を作らない／すぐ気づく」ための対策だが、
それでも並行作業がある限り、組み合わせが未検証のまま統合される可能性は残る。
ここを塞ぐのは個々の注意力ではなく、マージの仕組みである。

- **マージキュー**、または「base が最新であること」を要求するブランチ保護を使う。
  要求すべきは「各変更が緑」ではなく「**マージされる形の組み合わせが緑**」である。
- 全面導入が難しいなら、**鮮度の上限**だけでも設ける。
  「CI 完了から N 時間以上経った変更は、再実行してからマージする」。
  N は運用で決めればよく、閾値があること自体に効果がある。
- **auto-merge を使うなら、base 更新時の CI 再実行とセットにする**。
  再検証なしの auto-merge は「古い緑でのマージ」を自動化する仕組みになってしまう。
- **連続マージを直列化する**。滞留していた変更をまとめて投入するときは、
  1 本マージするたびに後続の CI を再実行する。マージキューはこれを自動化したものであり、
  導入していない環境では「連続マージしない」という運用規律がその代替になる。
  ここは個人の記憶力ではなく、**在庫している変更が互いに未検証であることを既定として扱う**のが要点。
- 統合ブランチへの push でも同じジョブを走らせ、壊れた状態を
  人間の手作業（誰かのローカル実行）ではなく CI が最初に見つけるようにする。
  すり抜けの検知時間を「数時間〜数日」から「1 ビルド」に縮められる。

### 六次対策: 「実際に効いている設定」を、実際の経路で観測可能にする

- 疎通確認のログは、**実際の呼び出しが通る解決層**で取る。
  別経路で取ったログは「繋がっている」ことの証明にならず、むしろ誤誘導する。
- 可能なら、解決の結果に**どの段で決まったか**（override / 設定ソース / 環境変数 / 既定値）を
  併記する。「既定値で決まった」が見えるだけで、この型の調査は数分で終わる。
- 段階移行の期間は、移行元と移行先の**両方の評価結果を並べて出す**と乖離が即座に見える。

### 採ってはいけない対策

- **落ちたテストの期待値を既定値に書き換える**。テストは「機能が有効なときの挙動」を
  検証しなくなり、偽陰性が確定する。落ちているのはテストではなく設定の到達性である。
- **落ちたケースだけを削除・skip する**。肯定ケースは注入経路の唯一の証明なので、
  それを消すと以後この型は永久に検出されない。
- **「フレークだった」で記録を閉じる**。決定的な失敗であり、再現条件（バイナリの入れ替わり）も
  特定できる。フレークとして数えると、原因が指標に残らず同じ構造が再発する。
- **本番の設定解決層を厳格化して未知キーで落とす**。障害時に設定が引けないと
  サービスが停止する。厳格化してよいのはテスト専用経路だけである。

## 一般化・チェックリスト

文字列キーで参照される設定を**改名**するとき:

- [ ] 旧名をリポジトリ全体で grep し、参照箇所（**テストコード・設定ファイル・デプロイ定義**を含む）を洗ったか。
- [ ] 参照側がキー文字列を複製していないか。複製しているなら、定数の共有に変えられないか。
- [ ] 改名後・参照側追随前の区間で、その設定を**与える手段が存在するか**を確認したか。
      新旧どちらの解決層にも到達しないなら、その区間は制御不能である。
- [ ] 段階移行中の疎通確認ログは、**実際の呼び出しが通る経路**を見ているか。

変更を**マージする**とき:

- [ ] その CI の緑は、**いつの base** で得られたものか。完了時刻とマージ時刻はどれだけ離れているか。
- [ ] 滞留していた変更なら、マージ前に CI を再実行したか。
- [ ] 同じ識別子・同じ設定に触れる**並行中の変更**が無いか確認したか（**自分の他の変更も含めて**）。
      ただしこの確認は取りこぼす前提で、下の仕組み側の項目を優先する。
- [ ] 滞留していた複数の変更を**連続してマージ**していないか。
      するなら、各マージの後に後続の CI を再実行したか。
- [ ] auto-merge を使うなら、base 更新時に再検証される設定になっているか。

設定を注入する**テストを書く**とき:

- [ ] **肯定ケース**（非既定値を期待するケース）が最低 1 本あるか。
- [ ] 否定ケース（既定値を期待）だけになっていないか。それは入力が届かなくても通る。
- [ ] キー文字列を手で書き写しているなら、その複製が壊れたときに**どこで気づくか**を答えられるか。

テストが**落ちたとき**:

- [ ] 同一バイナリで再実行して、決定的かフレークかを先に判定したか。
- [ ] 実測値が**すべて既定値**に揃っていないか（入力不着のサイン）。
- [ ] 同一テスト内で **PASS しているケースの内訳**を見たか。既定値期待ばかりなら偽陰性を疑う。
- [ ] 値が既定値になる**別の経路**が出すはずのログが、本当に出ていないことを確認したか。
- [ ] 落ち始めた周の直前で、被テストバイナリが入れ替わっていないか
      （新しいコードにしか無いログ行の初出位置で確定できる）。

## 教訓

- **安全側フォールバックは、障害には効くが設定ミスには効かない**。
  「取れなければ止める」は正しい設計だが、その正しさが設定ミスを正常動作に化けさせる。
  fail-safe を入れたら、**設定が届いているかを別の手段で観測**しなければならない。
- **不一致がエラーにならない境界は、テストだけが守っている**。
  文字列キーで疎結合した箇所は、型検査もコンパイルも通過する。
  そこを跨ぐ契約は、明示的なテストが無ければ誰も検証していない。
- **通ったテストの内訳を見る**。否定側の期待値だけで構成されたケースは、
  入力が一切届かなくても緑になる。「何本通ったか」ではなく「何を証明したか」で数える。
- **テストのための複製は、単一の情報源を壊す**。本番の内部に触れさせない配慮が、
  検証不能な写しを生むことがある。分離の代償として何を失うかを毎回意識する。
- **識別子の改名は、静的に検出できる契約変更である**。
  意味の反転と違い grep で見つかる。見つけられるのに見つけていないだけなので、
  手順化すればこの型は消える。
- **長時間ループでは「いつのコードか」を最初に確定する**。
  新しいコードにしか無いログ行の初出位置は、バイナリ入れ替わりの時刻を 1 行で示す。
- **CI の緑には賞味期限がある**。変更単位の CI は「過去のある時点の base での緑」しか示さない。
  base が進んでも再実行されない仕組みでは、滞留期間がそのまま結果の古さになる。
  「テストがあったのに、なぜ CI で止まらなかったのか」の答えは、たいていここにある。
- **注意力に依存する対策は、最も有利な条件でも失敗する**。同一人物が同一領域に対して
  同時に起票し、同じ朝に連続してマージした変更同士ですら、組み合わせの検証は抜けた。
  「関連する変更を確認する」は、確認相手が自分でも機能しない。防ぐのは仕組みの側である。
- **鮮度は機械的に測れる**。CI 完了時刻とマージ時刻の差は API から取れる値であり、
  「その緑は実際のマージ先を見ていない」を人の注意力に頼らず判定できる。
  検出できるのに測っていないだけ、という点で、この失敗は防げる側に属する。

## 関連

- [単独で検証できない単位への PR 分割が、統合時にしか現れない不整合を「フレーク」として噴き出させる](./contract-change-split-across-unverifiable-prs.md) —
  「各変更の CI は緑・統合状態だけ未検証」という同じ構図。あちらは**値の意味**が反転して
  期待値と実測値が補数関係になる型、こちらは**識別子**が一致せず実測値が一律に既定値へ落ちる型。
  診断のサインが逆なので、失敗値の分布で先に切り分ける。
- [テストランナーの結果キャッシュがヒットして「テストの副作用」が実行されない](./test-cache-hit-skips-server-startup-side-effect.md) —
  失敗の原因がテストコードの外（実行基盤・設定の到達性）にある型。
  `FAIL` 行の中身だけを読んでも原因に届かない点が共通。
- [月単位の日付演算が正規化で繰り上がり、「前月」が「当月」になる](./month-arithmetic-normalization-overflow.md) —
  フレークの顔をして落ちるが実は決定的、という同じ構図。
  切り分けの第一歩が「決定性の確認」である点も共通。
- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) —
  本件では、長時間ループが「設定の到達性が一度も検証されていなかった」ことを暴いた。
