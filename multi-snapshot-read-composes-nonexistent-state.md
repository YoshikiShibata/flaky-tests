# 複数スナップショットで組み立てる読み取りが「どの時点にも存在しなかった状態」を観測する

## 概要

1 つの結果を返す読み取り処理が、内部で複数回に分けてデータを読んでいる場合、**その読み取りが別々の時点（スナップショット）で行われていると、どの時点にも存在しなかった組み合わせを観測する**。

典型は「一覧を引く → 得られた ID で参照先を引く → 同じ ID で関連する件数を数える」という 3 段構成で、読み取りの合間に他リクエストの削除がコミットされると、

- 一覧には要素があるのに、その参照先が無い
- 参照先は取れたのに、関連件数が 0

という、**単一のコミット時点としては絶対に存在しない状態**が読める。

書き込み側が 1 トランザクションで原子的に削除していても防げない。壊れているのは読み取り側であり、原子性は「途中状態をコミットしない」ことしか保証しないため、**複数の時点を読む相手には何の保証も与えない**。

観測した不整合を「あり得ないデータ不整合」として内部エラー（HTTP 500 / gRPC INTERNAL）で fail loud させる実装と組み合わさると、**正常な並行操作に対してサーバーエラーを返す**バグになる。

E2E の並列実行は本番より衝突が起きやすいため、この欠陥は**低頻度のフレーク**という顔で先に現れる。テストの分離不足に見えるが、実体はサービス側のバグである。

## 発生条件

1. **読み取り処理が複数回に分けてデータを読む** — 一覧＋参照先、一覧＋集計、親＋子、本体＋権限情報、など
2. **各読み取りが独立したスナップショットで実行される** — リポジトリ／DAO のメソッドが内部で毎回それぞれ独立した読み取りトランザクションを開く実装（多くの ORM・DB ラッパの既定）
3. **読み取り対象を削除・更新する並行処理が存在する** — 運用操作、バッチ、他ユーザーのリクエスト、テストの後片付け
4. **組み合わせの欠損をエラーとして扱う** — 「参照先が無い」「件数が 0」を不整合として弾く実装だと即失敗する。落として返す実装なら症状は出ない（後述）

## なぜ起きるか

### 各読み取りが「別の時点」を読んでいることは呼び出し側から見えない

問題の核心は、**呼び出し側のコードには複数時点を読んでいる手がかりが無い**ことにある。

```go
// usecase から見ると、ただの関数呼び出しが 3 つ並んでいるだけ
items, err := repo.ListItemsByOwner(ctx, ownerID)   // 時点 T1
parents, err := repo.ListParentsByIDs(ctx, ids)     // 時点 T2
counts, err := repo.CountChildrenByIDs(ctx, ids)    // 時点 T3
```

各メソッドの内部で「読み取り専用トランザクションを開いて閉じる」処理が閉じ込められていると、シグネチャにも名前にもそれは現れない。結果として `T1 < T2 < T3` という事実が**コードのどこにも書かれていない**状態になる。

### 窓は読み取りと読み取りの「間」にある

N 回に分けて読むなら、窓は N-1 個ある。

```
T1: 一覧を読む          … 要素 X が返る（この時点では参照先も存在）
        ↓ ここで削除がコミット（窓 1）
T2: X の参照先を読む    … 無い → 「参照先が引けない」エラー
        ↓ ここで削除がコミット（窓 2）
T3: X の関連件数を数える … 0 → 「件数が 0」エラー
```

**同じ原因が、削除の入る位置によって別のエラーメッセージで現れる**。エラーメッセージだけを見ていると別々のバグに見えるため、原因の同一性に気づきにくい。

### 書き込み側の原子性では塞げない

削除側が「参照先と要素を 1 トランザクションで消す」設計でも、コミット済みの状態としては

- 削除前: 要素あり・参照先あり
- 削除後: 要素なし・参照先なし

の 2 状態しか存在しない。それでも読み取り側は前者と後者を**混ぜて**観測できる。**「書き込みが原子的だから読み取りも安全」は誤り**で、必要なのは読み取り側のスナップショット一貫性である。

### 「読み取りだけだからトランザクションは不要」という既定判断

多くのコードベースで、トランザクションは書き込みの道具として扱われる。規約に

- トランザクション内で外部 API を呼ばない
- トランザクションは短命に
- 書き込みは 1 トランザクションにまとめる

とだけ書かれていると、読み取り専用の処理でトランザクションを張るのは**大げさな逸脱**に見える。実際、あるサービスでは usecase 層のトランザクション呼び出し 121 箇所のうち、読み取り専用指定は 2 箇所しか無かった。**慣習が「読み取りは裸で読む」に固まっていると、この判断ポイント自体が意識に上らない。**

### fail loud 規約との相互作用

「理論上あり得ないデータ不整合は握り潰さず内部エラーで検知可能にする」という規約は正しい。しかし本パターンでは、

1. 実装者は「参照先の無い要素＝あり得ない不整合」と分類する
2. 分類した時点で「本当に起こり得ないか」の検討が終わる
3. 実際には**自分の読み方が作り出している**ので、正常な並行削除のたびに発火する

という順序で誤りが確定する。**fail loud は「起こり得ない」の判定が正しいことに全面的に依存する**。判定を誤ると、監視アラートを鳴らす仕組みが、そのまま平常時のエラー生成器になる。

## 症状と、症状が出ない場合

同じ読み取り構造でも、欠損の扱い方で症状が変わる。

| 欠損の扱い | 症状 | 検出可能性 |
|---|---|---|
| エラーにする（fail loud） | 稀に 500 / INTERNAL。E2E では低頻度フレーク | 顕在化する（本パターン） |
| 欠損した要素を除いて返す（degrade） | エラーにならない。返る件数が一瞬揺れるだけ | ほぼ検出されない |

**同じサービス内で両方の実装が混在していることがある**（例: 運用向け画面はエラー、利用者向け画面は除外）。片方だけ直せばよいのか、両方の仕様を揃えるべきかは仕様判断になるので、修正時に明示的に決める。

なお degrade 側は「エラーにならない」だけで、読み取りの一貫性が無い事実は変わらない。件数・合計・ページングの整合が問われる文脈では、そちらも同じ手当てが要る。

## なぜ普通のテストでは捕まらないか

- **単発呼び出しのテストでは絶対に踏めない**。窓は 1 リクエストの内部にあり、外から観測も注入もできない
- **レビューでも見えにくい**。読み取りだけのコードは「副作用が無い＝安全」と読まれる
- **静的解析にも掛かりにくい**。個々の呼び出しはどれも正しい

結果、検出器は事実上「並列実行される E2E を長時間まわしたときの低頻度フレーク」しか無い。**フレークの顔で現れる本番バグ**の典型で、再実行すれば消えるため、そのまま「環境要因」として処理されやすい。

## 誤診しやすい点

低頻度フレークとして現れたとき、次の誤診に流れやすい。

| 誤診 | 反証の取り方 |
|---|---|
| テストの分離不足（他テストのデータが混ざった） | エラーに現れる ID が**自テストが作ったもの**かを確認する。他テストのものなら分離の問題だが、自テストのものならテスト側では直せない |
| 残留データ（前回実行のゴミ） | 該当 ID が**ログ全体で 1 回しか出現しない**なら一過性。残留なら以降の実行でも繰り返し出る |
| エミュレータ／DB の劣化、長時間実行の副作用 | 連続実行の何周目で出たかに偏りが無いかを見る。周回に依らずランダムなら劣化仮説は弱い |
| 実装は正しいがテストの期待値が厳しすぎる | **仕様として 500 を返してよいか**を問う。「一覧を開いている最中に誰かが消したら 500」が許容仕様であることはまずない |

決め手は「エラーに出ている ID の出自」と「その ID の出現回数」で、どちらもログの grep だけで確定できる。

## 対策

### (a) 読み取りを 1 スナップショットに束ねる（本命）

一連の読み取りを読み取り専用トランザクションで囲む。多くの実装では、コンテキストにトランザクションを載せると各リポジトリメソッドがそれを再利用するため、**リポジトリ側のシグネチャ変更なしに**適用できる。

```go
err := tx.RunInTransaction(ctx, func(ctx context.Context) error {
    items, err = repo.ListItemsByOwner(ctx, ownerID)   // 同一スナップショット
    parents, err = repo.ListParentsByIDs(ctx, ids)     //     〃
    counts, err = repo.CountChildrenByIDs(ctx, ids)    //     〃
    return err
}, ReadOnly())
```

注意点:

- **結果の組み立て（純粋な計算）はトランザクションの外に出す**。中に入れる必然性が無く、トランザクションを不必要に長く見せる
- **読み取り専用トランザクションは再実行されない**エンジンが多い。書き込み用トランザクションと違い、クロージャ外の変数へ代入しても再試行で壊れない（逆に、書き込み用では壊れる。別パターンとして既知）
- **早期 return はトランザクション内でも安全**（書き込みが無いので部分適用が起こらない）。ただし「トランザクション内の途中 return を避ける」という書き込み向けのレビュー規約に機械的に引っかかるので、理由をコメントに残す

### (b) 欠損を仕様として許容する（degrade）

「参照先が引けない要素は結果から落とす」に変える。実装は簡単だが、**件数や集計の意味が変わる**ので仕様判断が要る。(a) が使えない構成（複数データストアをまたぐ等）では現実的な選択肢になる。

### 効かない対策

- **書き込み側の原子化** — 既に原子的でも起きる。窓は読み取り側にある
- **テストの後片付け順序の入れ替え** — テストからは直せない。本番でも起きる
- **テスト間の排他ロック** — E2E は緑にできるが、本番のバグは残る。**「テストが通ったから直った」と誤解する分だけ有害**
- **リトライ** — 稀に成功するので隠蔽できてしまうが、根治ではない

## 再現テストの作り方

窓が 1 リクエストの内部にあっても、**衝突する操作を意図的に並走させれば高確率で踏める**。以下は一般化した手順。

1. **自テスト専用のリソースを使う** — 共有リソース（全ユーザー宛・グローバル設定など）を作ると他テストの観測結果を汚す。自分専用の親子データなら、並列実行中の他テストに一切影響しない
2. **削除（衝突操作）と読み取りを並走させる** — 読み取りを数本のゴルーチンで叩き続けながら、対象を削除する
3. **複数試行する** — 1 試行あたりの命中率は「窓の幅 ÷ 読み取り 1 回の所要時間」オーダー。実測では **1 試行あたり 2 割前後**で、20 回の実行では 0〜14 試行目にばらついた。試行回数は取りこぼし確率から決める（p=0.2、25 試行で約 0.3%）
4. **空振り防止のガードを 2 つ入れる**
   - **対象が本当に読み取り対象に入っているか**（毎試行チェックする。ページングの都合などで対象が観測範囲から外れると、以降の試行は何も検証しない）
   - **衝突操作が本当に効いたか**（最後に「削除されたものが見えなくなっている」ことを確認する。削除が no-op でも「エラーが出ない」は成立してしまう）
5. **ループに上限を設ける** — 読み取りを無制限に叩き続けると、衝突操作が詰まったときに共有 DB を占有して他テストのフレーク源になる
6. **変異テストで検証力を確認する** — 修正を戻した状態で必ず落ちること、修正後は連続して通ることを実測する。例: **修正前 20 回の実行が 20 回とも失敗、修正後 10 回の実行が 10 回とも成功**

このテストは、修正後は「N 試行して 1 度もエラーが出ないこと」という確率的な保証になる。決定的な固定が欲しければ、DB 層で「読み取りトランザクションの途中に削除をコミットしても、同一トランザクション内の後続読み取りには見えない」ことを直接検証するテストを別に置く（こちらはスナップショットが固定される性質上、タイミング依存なしに書ける）。

## 検出方法（既存コードから探す）

コード検索で機械的に洗い出せる目印がある。

- **同じ ID 集合に対する読み取りが 2 回以上あり、それらがトランザクションの外にある**
  - 「一覧を引く → その ID で本体を引く → 同じ ID で件数を数える」の形
- **読み取り結果を突き合わせるときに「片方に無い」をエラーにしている箇所**
  - `if _, ok := m[key]; !ok { return error }` の形。ここが本パターンの発火点になる
- **読み取り専用トランザクションの使用率が極端に低い**
  - トランザクション呼び出し全体に対する読み取り専用の比率を数える。ほぼゼロなら、慣習として「読み取りは裸で読む」が固まっているサイン
- **リポジトリ層が呼び出しごとに読み取りトランザクションを開いているか**
  - 開いているなら、上記の形は全て候補になる

## 一般化したパターン

| 項目 | 内容 |
|------|------|
| 不整合の種類 | どの単一時点にも存在しなかった組み合わせの観測（read skew） |
| 壊れている場所 | **読み取り側**。書き込み側は正しくても発生する |
| 窓の位置・数 | 読み取りと読み取りの間。N 回読むなら N-1 個 |
| 症状 | 参照先が引けない／件数が 0 など、窓の位置ごとに別のエラーになる |
| 表面化 | 並列 E2E の低頻度フレーク（本番より衝突しやすいため先に出る） |
| 誤診しやすい先 | テストの分離不足・残留データ・エミュレータ劣化 |
| 決め手 | エラーに出る ID が自テスト由来か／その ID がログに 1 回しか出ないか |
| 対策 | 読み取りを 1 スナップショットに束ねる。次善は欠損の degrade（仕様判断） |
| 効かない対策 | 書き込み側の原子化・後片付け順序・テスト間ロック・リトライ |
| 再発防止 | 規約に「複数の読み取りで 1 つの結果を組み立てるなら 1 スナップショットに束ねる」を明記する |

## チェックリスト

1 つの結果を返すために複数回データを読む処理を書く・レビューするときは:

1. **この処理は何回データを読むか？** — 2 回以上なら本パターンの候補
2. **各読み取りは同じ時点を読むか？** — リポジトリ層が呼び出しごとにトランザクションを開いているなら、答えは No
3. **読み取りの合間に、対象を削除・更新する経路はあるか？** — 運用操作・バッチ・他ユーザー。無いと言い切れることは稀
4. **突き合わせで「片方に無い」をどう扱っているか？** — エラーにしているなら、正常な並行操作でそれが発火する
5. **「書き込み側が原子的だから安全」で止まっていないか？** — 読み取り側の一貫性は別問題
6. **「読み取りだけだからトランザクション不要」で判断していないか？** — 読み取りこそスナップショットのズレが露出する場所
7. **fail loud にする前に、その状態が本当に起こり得ないことを確認したか？** — 自分の読み方が作っていないか
8. **degrade する実装が別経路に存在しないか？** — 同じデータに対して仕様が割れていないか確認する

## 教訓

1. **原子的な書き込みは、複数時点を読む相手を守らない** — 整合性は書き手と読み手の両方で成立して初めて保たれる
2. **「読み取りだけだから安全」は誤り** — 副作用が無いことと、観測が一貫していることは別
3. **抽象化が時点の境界を隠す** — リポジトリのメソッド呼び出しは、内部でトランザクション境界を跨いでいても呼び出し側から見えない。**隠れているのは実装詳細ではなく保証の有無**
4. **同一原因が複数のエラーメッセージで現れる** — 窓ごとに症状が違うので、メッセージ単位で追うと別バグに見える。窓の位置で整理すると 1 つに畳める
5. **fail loud は「あり得ない」の判定精度に依存する** — 分類した瞬間に検討が終わるので、分類そのものを疑う手順を挟む
6. **並列 E2E は本番より先にレースを見つける** — 衝突頻度が桁違いに高いため、本番で顕在化する前に低頻度フレークとして出る。**フレークを「環境要因」で閉じると、この早期警告を捨てることになる**
7. **テストを緑にする手段と、バグを直す手段は別** — ロックや再実行はテストを緑にするが、本番の欠陥はそのまま残る
8. **規約が片側しか書いていないと、もう片側は既定判断に落ちる** — 「トランザクション＝書き込み」としか書かれていない規約は、読み取りの一貫性を検討させない。規約に 1 項目足すのが最も安いレバー

## 関連パターン

- [セットアップ・クリーンアップの順序が引き起こす孤立参照](./setup-cleanup-order-orphan-reference.md) — 同じ「参照先が引けない」症状だが、あちらは**テスト側**が中間状態をコミットする型で、対策は挿入・削除順序と排他。あちらの (b) で「走査側が複数スナップショットで読むなら排他を足す」と整理されている残余リスクが、本パターンでは主役になる。**書き込み側が完全に原子的でも起きる**ため、テスト側の対策では直らない点が決定的な違い
- [グローバル集計値の before/after 比較が並行書き込みで揺れる](./global-aggregate-before-after-assertion.md) — 「2 つのスナップショットの間に他がコミットする」構造は同じ。あちらは**テストが**2 時点を比較する型、こちらは**本番実装が**2 時点を組み合わせる型
- [keyset ページネーションの tie-breaker 欠如による行の取りこぼし](./keyset-pagination-missing-tiebreaker.md) — 間欠的な E2E 失敗がテストのフレークではなくサービス側の実装バグだった、という同じ構図。読み取りの正しさが壊れている点も共通
- [トランザクション再試行で使い回すモデルへの ID 書き戻しが INSERT を UPDATE に転落させる](./tx-retry-model-mutation-flips-insert-to-update.md) — トランザクションの性質（再試行）を誤解する型。本パターンの「読み取り専用は再試行されない」という非対称性と併読するとよい
- [長時間ランニングテストの重要性](./importance-of-long-running-tests.md) — 1 試行あたり数 % の欠陥は、短時間の実行では「無い」ように見える
